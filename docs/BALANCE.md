# Balance & Logic Notes (GDD 고도화 결정 사항)

기획서(`docs/GDD.md`)의 공식은 그대로 유지하고, 기획서가 비워둔 수치/규칙과 2차 요청 사항(D~S 등급, 메인 영웅 직급 승진, 강화 카드, 일일 업무, 광고)을 아래처럼 확정했습니다.
모든 상수는 `src/config/balance.js` 한 곳에 있고, `tests/`에서 검증됩니다.

## 1. GDD 공식 (그대로 사용)

| 항목 | 공식 |
| --- | --- |
| 스탯 업그레이드 비용 | `floor(10 × 1.12^(Level-1))` |
| 몬스터 HP | `floor(50 × 1.18^(Stage-1))` |
| 기본 골드 | `floor(5 × 1.15^(Stage-1))` |

## 2. 방치 보상 (사용자 결정 반영)

- 켜두면 실제 전투 수익 100%. 탭이 스로틀돼도 시뮬레이션은 고정 스텝으로 따라잡음.
- 꺼두면 `goldPerSec(maxStage) × min(초, 10h) × 0.6`. 복귀 시 Excel 다이얼로그로 정산.
- 광고(임시 5초 카운트다운, SDK 미연동): 하루 3회. 복귀 정산 2배 / 즉시 1시간 방치 보상(100% 효율).
- `goldPerSec(stage) = baseGold × goldMult × min(5, 1 / (HP/DPS + 1s 리스폰 + 1s 이동))`

## 3. 카드 등급 D ~ S

| 등급 | 확률 | 기본 ATK/HP | ★승급 조각 (★1→2 … ★4→5) | 조각→카드 |
| --- | --- | --- | --- | --- |
| D 일반 | 45% | 6 / 100 | 10 / 20 / 40 / 80 | 1장 |
| C 희귀 | 30% | 8 / 130 | 15 / 30 / 60 / 120 | 2장 |
| B 고급 | 17% | 11 / 160 | 20 / 40 / 80 / 160 | 4장 |
| A 영웅 | 7% | 15 / 200 | 25 / 50 / 100 / 200 | 8장 |
| S 전설 | 1% | 20 / 250 | 30 / 60 / 120 / 240 | 16장 |

- 천장: 50회 내 A 이상(소프트), 100회 내 S(하드). 획득 시 카운터 리셋.
- 새 카드 → 즉시 ★1 해금(사용자 확인). 중복 → 조각 5~10개.
- StarMult ★1~★5 = `1 / 1.25 / 1.6 / 2.1 / 2.8`. 스킬은 ★2 해금, ★4 위력 ×1.5, ★5 캔버스 글로우.
- 로스터 20장: D6 · C5 · B4 · A3 · S2 (역할 탱커/근접/원거리/힐러 혼합).

## 4. 메인 영웅 (김인턴) 직급 승진

| 티어 | 직급 | 등급 | 역할 | 필요 강화 카드 | 필요 클리어 |
| --- | --- | --- | --- | --- | --- |
| 0 | 인턴 | D | 근접 | - | - |
| 1 | 사원 | C | 근접 (스킬 해금) | 20 | Phase 1-5 |
| 2 | **대리 (트랙 선택)** | B | 영업대리=근접 강타 ×4·치명 / 재무대리=원거리 광역 ×1.6·골드 +8% / 총무대리=탱커·조항의 방패 ×3 | 60 | Phase 2-5 |
| 3 | 과장 | A | 영업과장=강타 ×5·팀 리더십 / 재무과장=광역 ×2·보스 +30% / 총무과장=경영 지시 ×2.2·철벽 (스킬 ×1.5) | 150 | Phase 3-10 |
| 4 | 부장 | S | 영업부장=근접 강타 ×6.5 / 재무부장=원거리 광역 ×2.5 / 총무부장=탱커 + 전체 기절 | 400 | Phase 5-10 |

- 메인 영웅은 뽑기 풀에 없고, ★승급 대신 직급 승진으로 등급이 오릅니다. 트랙(영업/재무/총무)은 사원 직후 대리 승진 때 고르며, 이후 승진은 같은 트랙 안에서만 진행됩니다(되돌릴 수 없음, 열린 질문 참고).
- 주인공은 어느 직급·트랙에서도 같은 인물(젊은 남성, 검은 단발)이어야 합니다. 일러스트 프롬프트와 도트 스펙 모두 이 규칙을 따르고, 트랙 색(영업 빨강·재무 청록·총무 주황)만 직급과 함께 짙어집니다.
- 직급이 바뀌면 카드 일러스트·역할·포메이션 슬롯이 즉시 바뀝니다.

## 5. 강화 카드 (조각/카드 재활용)

- 재화 `cards`. 획득: 조각 변환(등급 가중치 표 참고), 카드 방출(파티 밖 카드를 통째로 → `(10 + 조각) × 가중치`), 전체 업무 완료 보너스.
- 사용: 모든 영웅 강화 `+1` = ATK/HP +4% (최대 +50, 비용 `floor(10 × 1.2^강화)`), 메인 영웅 직급 승진.
- "승계된(하위/중복) 카드는 강화 카드로 사용" 요구를 조각 변환 + 카드 방출로 구현.

## 6. 일일 업무 (검토 탭)

- 자정(로컬) 리셋. 골드 보상은 상대값: `baseGold(maxStage) × N`.

| 업무 | 목표 | 보상 |
| --- | --- | --- |
| 오류 100건 처리 | 처치 100 | 보석 20 + 골드 ×60 |
| 인사 평가 10회 | 업그레이드 10 | 보석 15 + 골드 ×40 |
| 데이터 가져오기 | 뽑기 1 | 보석 30 |
| 시트 5장 마감 | 스테이지 클리어 5 | 보석 30 + 골드 ×80 |
| 긴급 티켓 처리 | 보스 1 | 보석 50 |
| 역량 강화 3회 | 강화 3 | 보석 15 + 골드 ×40 |

- 출근 도장: 보석 100 + 골드 ×100. 전체 완료 보너스: 보석 100 + 강화 카드 20.
- 하루 보석 수입 ≈ 360 + 스테이지 클리어 → 2~4일에 10연차 1회 페이스.

## 6-1. 3차 요청 반영 (데스크톱 · 64px · 특성 · 재시작 · 몬스터 다양화)

- **레이아웃**: 엑셀 스킨 유지, 데스크톱 폭(≈1240px). 왼쪽 시트(전투 캔버스 832×416 = A1:M8 셀 프레임), 오른쪽 Excel 작업 창("파티 관리")에 강화·회사 업그레이드·이번 스테이지 출현 몬스터.
- **보스 키(Esc)**: 레이아웃은 그대로, 전투 캔버스 영역만 텍스트 표로 바뀜. 상단 재화 숫자·카드 그림·아이콘만 감춤.
- **스프라이트**: 32×32 손 도트를 2배 표시(64px). 영웅은 사용자 참고 이미지 기준 **2등신 치비**(큰 머리카락 덩어리 + 어두운 반점, 파란 가로 막대 눈, 작은 몸통·손), 오른쪽을 바라봄, 색상별 어두운 외곽선. 몸통 6포즈 · 머리 10종 · 액세서리 17종 · 소품 14종 · 무기 4종×3포즈를 픽셀 맵으로 합성(src/data/heroArt.js). 몬스터는 GBA 포켓 도트 느낌의 **생물형 손 도트 14종**(순환참조 뱀, 병합셀 거북, #REF! 가시 도마뱀, 매크로 벌레고양이, 유령, 종이 새, 차트 골렘, 모래시계 달팽이, 자물쇠 딱정벌레, 무당벌레, 구름 강아지, 케이블 꼬리 마우스, 복붙 원숭이, 마감 황소) + 페이즈별 팔레트 재색칙, 왼쪽(영웅 쪽)을 바라봄(src/data/monsterArt.js). 보스 긴급 티켓은 유지.
- **특성(패시브) 10종**: 크리티컬 / 공속 / 파티 골드 / 피해 감소 / 흡혈 / 보스 피해 / 파티 공격력 오라 / 클리어 보석 / 추가 재생 / 스플래시. 모든 카드와 메인 직급에 1개씩 부여.
- **사냥 / 도전 모델 (4차 요청)**: 기본 상태는 현재 스테이지 **무한 자동 사냥**(골드·처치만 쌓임). 리본의 **"Phase X-Y 도전"** 버튼 또는 **자동 진행** 체크가 켜져 있으면 다음 스테이지 도전 전투가 벌어지고(20킬, 보스는 30초 내 처치), 성공하면 그 스테이지가 새 사냥지가 되며 자동 진행 시 곧바로 다음 도전. **실패(전멸·보스 타임아웃)** 하면 자동 진행이 꺼지고 직전 스테이지에서 자동 사냥으로 돌아감 → 강화 후 다시 도전. 도전 중 버튼은 "도전 중단". 사냥 중 전멸은 그 자리에서 재시작. Phase 1-1 도전 중 전멸은 튜토리얼로 진행도 유지. 보스 스테이지를 사냥지로 삼으면 보스가 아닌 일반 몬스터가 나옴. 방치 수익은 사냥 중인 스테이지 기준.
- **몬스터 다양화**: 12종 형태 × 10종 팔레트. 페이즈마다 3종 풀 + 팔레트가 바뀜(예: Phase 1 붉은 순환참조/병합셀/#REF!, Phase 2 파란 #REF!/바이러스/유령 참조 …). 5스테이지부터 엘리트(왕관, HP ×2.5, ATK ×1.5, 골드 ×3) 출현 확률이 스테이지당 2%씩 올라 최대 30%.
- **보상 상승**: 처치 골드 1.15^스테이지, 첫 클리어 보석 10 + 강화 카드(페이즈 × 2), 보스 첫 클리어 보석 50. 엘리트는 골드 3배.

## 6-2. 연출 · 에셋 파이프라인 (5차)

- **이펙트 시스템**: 근접 기본 공격 베기 궤적, 원거리 발사 섬광, 스킬별 확산 링(광역 정리·경영 지시)과 파티 오라(팀 싱크), 힐 스파클, 타격 퍼프, 기절 별, 엘리트 등장 링, 화면 흔들림(보스 등장 8 · 보스 처치 14 · 궁극기 10).
- **원거리 몬스터**: 손상된 시트(종이), 폭주 차트(막대), 커서 도둑(화살), 동기화 충돌(빗방울), 응답 없음(모래)은 4셀 거리에서 투사체를 던지고, 투사체가 도착할 때 피해가 들어감.
- **등장·사망**: 몬스터 팝인(오버슈트), 사망 시 납작 눌리며 파편 파티클, 영웅 사망은 뒤로 넘어지며 페이드. 처치 골드는 코인 파티클과 `+Ng` 표시.
- **배너**: 도전 시작(파란 띠), 클리어(초록 띠), 자동 사냥 전환(회색), 보스 컷인(붉은 사선 띠 + 보스 초상 + 타이머 안내).
- **CC0 도트 팩 적용 (8차)**: 0x72 DungeonTileset II v1.7(CC0)의 영웅 10종(16×28, idle 4·run 4·hit 1)과 몬스터 16종·대형 3종을 `src/data/packSprites.js`로 연결. 영웅 28명은 10개 베이스 + hue 회전으로 구분, 몬스터 14종은 시트 생물에 매핑하고 페이즈마다 hue 36°씩 회전, 보스는 대형 데몬. 몬스터는 좌우 반전해 영웅 쪽을 봄. 시트가 로드되지 않으면 기존 코드 생성 도트로 자동 대체.
- **에셋 파이프라인**: `assets/sprites/manifest.json`에 PNG 시트를 등록하면 해당 id의 영웅/몬스터/보스가 코드 생성 도트 대신 시트를 사용(전투·카드·초상·아이콘 모두). 규격은 `assets/sprites/README.md`. 시트가 없는 id는 그대로 생성 도트 → 한 캐릭터씩 교체 가능.

## 6-3. 횡스크롤 던전 라인 전투 (9차)

- **한 줄 대열**: 영웅 5명은 화면 왼쪽에 `ㅁㅁㅁㅁㅁ` 한 줄(x=400부터 60px 간격, 탱커→근접→힐러→원거리 순)로 서서 제자리를 지킨다. 웨이브 사이에는 던전 벽·바닥이 150px/s로 흘러가며 영웅이 달리는 모션을 재생해 "전진하는 느낌"만 준다(1.6초, 스테이지 시작은 1초).
- **웨이브**: 이동이 끝나면 `min(MAX_MONSTERS, 3 + floor(stage/10))` 마리가 오른쪽 밖에서 걸어와 대열 앞에 늘어선다(근접은 34px 간격으로 순서대로, 원거리는 3.6셀+0.3셀×순번 뒤). 보스 스테이지 도전이면 보스 1마리.
- **공격 타이밍**: 근접·탱커 영웅은 대열 어디에 서 있든 **도착한(arrived) 몬스터**를 향해 대시 → 0.16초 뒤 타격(베기 이펙트) → 0.45초 안에 제자리 복귀. 원거리·힐러는 투사체를 쏘고 도착 시 피해. 몬스터 근접은 최전방 영웅을 때리고, **원거리 몬스터(및 보스 35%)는 살아 있는 영웅 중 무작위**를 노린다.
- **던전 배경**: 0x72 타일(벽 상단/중단, 배너 4색, 분수 애니, 기둥, 바닥 8종)을 스크롤 오프셋에 맞춰 그리고 페이즈별 hue 틴트·비네트. 무기 오버레이는 사용자 요청으로 제거.
- **Tiny Creatures 몬스터 12종 추가**: 총 26종, 페이즈마다 3종씩 풀에 들어가며 눈알/구슬/닭/로봇은 원거리.

## 6-4. 보스 3종 · 업적 · 도감 · 자동 합계 유지 (10차)

- **보스 로테이션**: 페이즈마다 보스가 바뀐다(`bossForStage`). 긴급 티켓(마감 데몬, 35% 불덩이 원거리) → 야근 좀비 부장(HP ×1.25 · ATK ×0.9 · 느림, 3번째 공격마다 앞 두 명 휩쓸기 90%) → 갑질 거래처 오우거(ATK ×1.15, 4번째 공격은 전원 발구르기 50%) → 반복. 컷인 배너와 스테이지 바에 보스 이름·패턴 설명 표시.
- **업적(누적)**: 검토 탭 하단. 처치·최고 스테이지·보스·뽑기·강화·누적 골드·플레이 시간·보유 영웅 8종, 단계별 보석 보상(20~500). 일일 업무와 달리 초기화되지 않으며 `state.achievements[id]`에 수령 단계 수를 저장. 수령 가능 업적이 있으면 검토 탭 점.
- **도감 보너스**: 보유 영웅 1종마다 파티 ATK +1% · 골드 +1%, 보유 영웅의 별 1개마다 ATK +0.5% (`BALANCE.COLLECTION`). 남는 카드로 별을 올리는 행동에 파티 전체 가치를 부여.
- **자동 합계 유지**: 리본/설정 토글. 켜두면 1초마다 "자동 합계"(가장 싼 파티 업그레이드 반복 구매)를 실행. 기본 꺼짐.
- **연출**: 궁극기(경영 지시) 시전 시 보라색 사선 컷인 + 시전자 3배 스프라이트. 영웅 피격 시 0x72 hit 프레임 사용. 스킬 쿨다운이 정의(SKILLS[type].cooldown)에서 읽히지 않아 한 번만 발동되던 버그 수정.

## 6-5. 보물 상자 · 회사 이전 · 효과음 (11차)

- **보물 상자**: 일반 웨이브에 6% 확률로 상자가 걸어 들어온다(HP는 일반 몬스터의 60%, 공격 안 함). 처치 시 `강화 카드 +페이즈 수`, `보석 3~8`. 30%는 **미믹**(HP ×1.6, ATK ×1.3 근접)으로 물기 시작할 때 입을 벌린 프레임으로 정체가 드러난다. 업적 "보물 사냥꾼".
- **회사 이전(프레스티지)**: 최고 클리어 30(Phase 3-10) 이상이면 가능. 지분 = `floor((maxCleared/10)^1.5)` (30→5, 50→11, 100→31), 1주당 파티 ATK·골드 영구 +3%. 초기화: 스테이지·골드·영웅 레벨·회사 업그레이드. 유지: 보유 영웅·별·강화·강화 카드·보석·직급·업적·통계. 업적 "회사 이전". 리본에 지분 표시.
- **효과음**: 오디오 파일 없이 WebAudio 합성(`src/ui/Sound.js`). 타격·피격·처치·골드·스킬·궁극기·보스 등장·클리어·뽑기·상자·전멸·업그레이드·회사 이전. 기본 꺼짐, 보스 키가 켜지면 항상 무음. 소리별 최소 간격으로 폭주 방지.
- **바닥 소품**: 벽 하단과 화면 아래에 나무상자·플라스크·코인이 월드 좌표에 고정되어 스크롤.

## 6-6. 엑셀 위장 고도화 (12차)

- **리본 = 리본**: 리본 탭(홈·삽입·데이터·검토·보기)이 시트를 바꾸는 대신 Excel처럼 **리본 내용**을 바꾸고, 해당 시트로도 이동한다. 그룹 라벨(전투 / 시트 / 외부 데이터 가져오기 / 천장 / 정렬 및 필터 / 도감 보너스 / 업무 검토 / 표시 / 확대·축소) 표시. 중복이던 **수식 탭과 수식 시트 탭은 제거**.
- **파일 = Backstage**: 파일 탭은 Excel 백스테이지(녹색 좌측 내비: 정보 · 옵션 · 저장/내보내기 · 게임 공식 · 통합 문서 삭제)로 열리고 ⟵ 또는 Esc로 닫힌다. 통계·회사 이전은 정보, 설정은 옵션에 배치.
- **빠른 실행 도구 모음**: 제목 표시줄에 "자동 저장 ● 켬" 필, 💾(Ctrl+S) 즉시 저장, ↶ ↷(토스트). 상태 표시줄에 선택 셀·계산 모드·보기 아이콘·확대/축소 슬라이더.
- **셀 선택**: 전투 캔버스를 클릭하면 Excel 선택 상자(녹색 테두리 + 채우기 핸들)와 열/행 머리글 강조, 이름 상자에 `F5` 같은 셀 참조, 수식 입력줄에 그 셀의 내용 수식 — 영웅이면 `=HERO("김인턴", LV, ATK, HP)`, 몬스터/보스/상자면 `=MONSTER(...)`. 영웅 셀 더블클릭은 상세 창. 커서는 셀 커서.
- **눈금선**: 보기 탭/옵션의 눈금선 토글(기본 켬)로 전투 화면 위에 64×52 셀 눈금을 희미하게 그린다.
- **파티 자동 편성**(데이터 탭): 보유 영웅 중 ATK 상위 4명 + 김인턴, 탱커·힐러가 있으면 1명씩 보장.

## 6-7. 스테이지 · 전투 · 이펙트 · 카드 (13차)

- **층(Phase) 이름과 테마**: 10개 층 이름(인사팀 지하 창고 → 회계팀 서버실 → … → 회장실 금고, 11층부터 "2차")과 층별 색조/틴트(`src/data/stages.js`). 스테이지 바와 도전 배너에 표시.
- **스테이지 변형**: x-5 **야근 러시**(몬스터 +2, 이동 속도 +30%, 골드 ×1.3), x-8 **감사 기간**(엘리트 확률 ×3, 골드 ×1.2), x-10 보스. 배너에 설명, 스테이지 바 힌트에 표시.
- **콤보**: 영웅이 피해를 주면 콤보 +1, 3초 무타격이면 사라지고 영웅이 맞으면 끊긴다("COMBO ×N 끊김"). 콤보당 피해 +0.5%, 최대 +25%(50히트). 캔버스 우상단 `N HIT · =COUNTIF(HITS)` 카운터, 색은 흰→노랑→빨강.
- **보스 텔레그래프**: 다음 공격이 특수 패턴(휩쓸기/발구르기)이면 보스 머리 위에 붉은 "!"가 점멸. 궁극기 시전 시 화면 백색 플래시(0.18초).
- **공격 이펙트**: 모든 타격에 방사형 임팩트 스파크(스킬·치명타는 크게), 근접 베기 색은 영웅 등급색(각성은 큰 베기), 대시 중 잔상 2장, 투사체 꼬리 3점, 치명타 16각 별 버스트.
- **각성(✦)**: ★5 카드에 강화 카드(D 60 · C 100 · B 160 · A 260 · S 400)로 1회. ATK/HP +25%, 특성 효과 ×1.5(치명 22.5%, 철벽 30% 등), 스킬 위력 ×1.25. 카드에 금색 이중 테두리·"각" 보석, 전투 중 금빛 오라. 메인 영웅은 직급 승진으로 대체.
- **승급 결과 창**: ★ 승급 시 카드 팝 애니메이션과 전/후 ATK·HP, 스킬 해금/강화·각성 가능 안내.

## 6-8. 워크시트 레이아웃 (14차)

- 모든 시트가 진짜 워크시트처럼 보인다: 모서리 셀, 시트 폭 전체의 열 문자(A…), 높이 전체의 행 번호, 22px 셀 눈금 배경. 전투 캔버스는 A1:M8에 삽입된 개체(그림/차트)처럼 앉아 있다(1~8행은 52px).
- 9행은 스테이지 상태 **키/값 셀**(모드 · 스테이지 · 출현 · 처리 · 힌트). 처리 진행도는 Excel **데이터 막대** 조건부 서식 모양(파란 그라데이션). 10행부터 활동 로그가 행으로 쌓인다(A열 행 번호, 종류별 글자색 = 조건부 서식 느낌, 시각은 툴팁).
- 제목 표시줄: 중앙 검색 상자(Alt+Q), 우측 사용자 아바타 "김". 리본 탭 줄 우측 "🗨 메모 · 👤 공유 ▾", 리본 축소 ˄. 수식 입력줄: 이름 상자 ▾, ✕ ✓ fx, 확장 ˅. 상태 표시줄: "접근성: 계속 진행 가능".

## 6-9. 로직 고도화 (15차)

- **승산(forecast)**: `ratio = (파티 DPS / 적 총 HP) ÷ (적 DPS / 파티 총 HP)`. 헤드리스 시뮬(7×6×3 조합)로 보정: 일반 스테이지는 ratio 2→0%, 10→100%, 보스는 3→0%, 15→100%(로그 보간). 보스 예상 처치 시간이 제한의 90%를 넘으면 15%로 캡. 리본 도전 버튼 아래 "승산 N% · 유리/접전/불리" 표시.
- **안전 자동 진행**(기본 켬): 클리어 후 다음 스테이지 승산이 35% 미만이면 자동 진행을 멈추고 현재 스테이지 사냥. 이후 2초마다 재평가해 강화·승급 등으로 승산이 오르면 자동으로 도전 재개. 옵션에서 해제 가능.
- **엘리트 접사(affix)**: 엘리트마다 1개 — 신속(속도·공속 +50%), 단단한(HP ×1.5), 재생(초당 2%), 폭발(사망 시 최전방 영웅에게 ATK ×3), 보호막(최대 HP 50%의 보호막을 먼저 소모, 파란 바). 이름 위에 접사 라벨.
- **타겟팅**: 근접·탱커는 도착한 몬스터 중 가장 가까운 적. 원거리·힐러는 보스 → 엘리트(HP 비율 낮은 순) → 가장 죽기 직전인 적(보호막 포함) 순으로 집중해 처치 속도를 올린다.
- **스킬 사용 판단**: 힐은 누군가 HP 75% 미만일 때만, 버프는 적 2마리 이상 또는 보스일 때만, 광역은 도착한 적이 있을 때만(아니면 0.5초 후 재판단). 강타는 보스 → 엘리트 → 최대 HP 적을 노림.
- **10연차 보장**: 10행 가져오기의 마지막 1장은 A 이상 미출현 시 A로 승격(천장 카운터도 갱신). 결과 카드에 "보장" 표시.
- **일일 업무 로테이션**: 풀 9종(기존 6 + 보물 상자 개봉 · 감사 대응(엘리트 3) · 연속 처리(콤보 20)) 중 날짜 시드로 6개(처치는 고정) 선택. 저장에 `daily.quests` 기록, 날짜 바뀌면 재추첨.

## 6-10. 16차: 영웅 28명 · 격노 · 차트 시트 · 컨텍스트 메뉴

- **영웅 +8 (총 28)**: D IT 헬프데스크(원거리·빠른 손놀림), 미화 여사님(힐러·행운) · C 영업팀 강과장(근접·영업 마인드), 법무팀 윤대리(탱커·보고서 특화) · B 기획팀장(원거리·팀 리더십), 디자인팀장(힐러·빠른 손놀림) · A CMO(원거리·행운) · S 창업자(근접·날카로운 지적, 궁극기 ×3.5). 도감 업적 단계 5/12/20/28.
- **투사체 다양화**: 원거리·힐러 영웅마다 던지는 사무용품이 다름(종이·막대·화살·물방울·모래).
- **보스 격노**: HP 50% 이하에서 1회 — ATK ×1.2, 이동 ×1.3, 공격 간격 ×0.7, 붉은 오라·배너·화면 흔들림.
- **통계_차트 시트**: 5초마다 샘플(최근 10분, 세션 메모리)로 골드/분 · 실측 vs 이론 DPS · 스테이지 진행 꺾은선, 파티 ATK 막대 — Excel 차트 개체 스타일(제목·눈금선·축·범례).
- **엑셀 UI**: 열 문자·행 번호 고정(스크롤해도 유지), 전투 화면 우클릭 컨텍스트 메뉴(영웅: 상세/강화/파티 · 몬스터 정보 · 잘라내기/복사/붙여넣기 · 도전 · 자동 합계 · 눈금선 · 보스 키 · 셀 서식…), 인사 명단 ATK 열 데이터 막대.

## 6-11. 17차: 마일스톤 · 캐릭터 프로필 · 일러스트 파이프라인

- **마일스톤(자동 지급)**: 스테이지 5마다(5~100) 보석 30+10×페이즈(보스 층은 60+), 강화 카드 2×페이즈(보스 4×); 김인턴 레벨 10마다(10~100) 보석 20+Lv, 카드 2×(Lv/10); 파티 레벨 합계 50/100/200/300/500. 도달 즉시 지급 + 금색 배너 + 토스트, 검토 탭에 다음 목표(진행 데이터 막대)와 최근 달성 3개. 회사 이전 후에도 유지(1회성).
- **캐릭터 프로필**: 28명 + 김인턴에 별칭·부서·한 줄 소개·등장 대사·궁극기 대사(`src/data/profiles.js`). 상세 창에 프로필 블록, 뽑기 신규 카드에 등장 대사, 궁극기 컷인에 대사.
- **카드 일러스트 파이프라인**: `assets/cards/manifest.json`에 등록된 영웅은 카드·초상·뽑기에서 픽셀 아트 대신 일러스트(cover, 얼굴 위쪽 기준). 미등록은 픽셀 유지 → 한 명씩 교체. `docs/ART_PROMPTS.md`에 공통 스타일 가이드 + 영웅별 생성 프롬프트(`node scripts/artPrompts.mjs`).

## 6-12. 18차: 수집 연출 · 스프라이트 정체성

- **뽑기 연출**: 카드가 Excel 로고 뒷면(등급별 색: S 금색, A 보라)으로 뒤집혀 있다가 0.17초 간격으로 차례로 공개. S는 잭팟 플래시 + 전용 사운드, A는 보라 글로우. 그리드 클릭으로 일괄 공개. 신규 카드에 등장 대사.
- **홀로그램 카드**: A·S·각성 카드에 움직이는 사선 광택(hover 시 빨라짐).
- **즐겨찾기(♥)**: 상세 창·우클릭 메뉴에서 토글, 명단 맨 앞 정렬, 카드에 ♥ 표시. 저장됨.
- **스프라이트 정체성(`src/data/heroSkins.js`)**: 0x72 베이스 10종에 전역 hue-rotate 대신 **부위별 팔레트 교체** — 베이스마다 머리/의상/포인트 색 군집을 표로 두고 영웅 팔레트(H/B/W)로 바꾼다(명도 차 유지). 대머리는 머리 군집을 피부색으로. 여기에 look의 acc/acc2/prop을 **16×28 픽셀 소품**(안경·선글라스·헤드셋·왕관·안전모·모자·넥타이·사원증·배지·수염·콧수염·커피·꽃·귀걸이·스카프·클립보드)으로 눈/정수리/턱/가슴 앵커에 찍는다. 달리기 프레임의 상하 흔들림은 프레임별 최상단 행 차이로 보정. 투구 베이스(knight)는 수염류 생략. 리뷰용 대조표: DevTools에서 `(await import('/scripts/contactSheet.js')).contactSheet()`.

## 6-15. 21차: Animagine XL 4.0로 생성한 블루 아카이브 톤 카드 (채택)

- 사용자가 pollinations 결과("3D 같다")와 벡터 초상을 모두 거부 → 애니 특화 모델이 필요. Hugging Face 공개 Space **Asahina2K/animagine-xl-4.0**(Gradio API, 익명 ZeroGPU)를 `scripts/genCardsHF.mjs`로 호출해 35장을 `assets/cards/<id>.png`(832×1216)로 생성. 프롬프트는 Danbooru 태그식: `1girl/1boy, solo, <머리색> hair, <스타일>, <소품 태그>, <등급 의상>, <재킷 색> jacket, <역할 포즈>, looking at viewer, cowboy shot, blue archive style, halo, flat color, cel shading, clean lineart, anime coloring, simple background, masterpiece, best quality`. 네거티브에 3d/realistic/photo/watermark. 스타일 프리셋 `Anim4gine`, Euler a, 28 steps, guidance 5, 시드 `1 + id 길이`.
- 호출 흐름: `POST /call/generate` → `GET /call/generate/{event_id}`(SSE) → `complete` 이벤트의 `image.url` 또는 `/file=` 경로에서 PNG 다운로드. 한 장 40~60초, 실패 시 20초×n 재시도.
- 게임 반영: 세로(전신) 일러스트는 상세 창에서 **세로 초상(contain)**으로 전체가 보이고, 카드/뽑기는 얼굴 우선 cover. `drawArtContain` 추가, `portraitCanvas`가 세로 그림이면 세로 캔버스를 만든다.

## 6-14. 20차: AI 생성 카드 일러스트 (반사실 톤, 미채택)

- 사용자 요청("블루아카 같은 일러스트")에 따라 `scripts/genCards.mjs`가 로그인 없는 무료 텍스트→이미지 엔드포인트(pollinations.ai)로 영웅 28 + 김인턴 직급 7 = 35장을 `assets/cards/<id>.jpg`(768×896)로 생성. 프롬프트는 **스타일 우선**(Blue Archive style, 2D anime, flat cel shading, halo, pastel bg) → 성별 → 소품(look) → 머리색+스타일 → 팔레트 의상(hex 대신 색 이름) 순. 시드는 `7 + id 길이`로 고정해 재생성이 재현됨(`SEED=` 환경변수로 변경).
- manifest 항목이 `{ file, crop: [0,0,1,0.94] }` 형태로 확장되어 하단 워터마크 띠를 잘라 그린다(`cardArt.js`의 crop 지원). SVG 벡터 초상(6-13)은 생성 실패 시 대체용으로 남겨둔다.
- 특정 캐릭터만 다시 뽑기: `node scripts/genCards.mjs --force ceo` (또는 `SEED=12 ...`).

## 6-13. 19차: 카드 일러스트 35장 (SVG 생성)

- `scripts/portraits.mjs`가 영웅 28명 + 김인턴 직급 7종의 **반신 초상 SVG(512×512)**를 만들어 `assets/cards/`와 manifest에 기록. 스타일: 플랫 벡터 치비 — 큰 눈(성별에 따라 속눈썹·크기), 눈동자 색은 팔레트 W, 머리 스타일 10종(short/bob/long/bun/side/spiky/curly/grey/bald/cap, 여성 short는 bob), 머리 광택, 팔레트 의상(임원·팀장은 블레이저 라펠), 소품(안경·선글라스·헤드셋·왕관·안전모·모자·꽃·귀걸이·수염·콧수염·넥타이·사원증·배지·앞치마·후디·스카프·커피), 등급 배경(색·격자·A/S 스파클·등급 테두리). 표정은 특성으로(날카로운 지적/보고서 특화 = 웃음, 철벽/영업 = 무표정).
- 로더(`cardArt.js`)는 SVG를 <img>로 읽어 카드·초상·뽑기 연출에 cover로 그린다. PNG로 덮어쓰면 즉시 교체.

## 6-16. 22~23차: 세계관·배경 · 캐릭터 34명 · 일괄 레벨업

- **세계관**: 어느 날 도시에 스프레드시트 괴물이 나타났고, 회사 직원들이 모여 본사까지 열 개 지구를 되찾는다. 전투 배경은 던전 타일에서 **폐허가 된 도시**(`src/ui/cityBackdrop.js`)로: 하늘 그라데이션+저물어 가는 해, 원경 실루엣(0.15), 파손된 건물(0.45, 불 켜진/깨진 창, 균열, 연기), 인도 슬래브와 아스팔트(1.0, 중앙선·균열), 콘크리트 잔해·철근·버려진 차·휘어진 가로등·흩날리는 서류, 재 입자. 층(Phase) 이름은 지구 이름으로: 무너진 오피스 거리 → 불타는 상업 지구 → 멈춘 지하철 입구 → … → 본사 옥상 결전. 지구마다 하늘·건물·창 색 테마.
- **캐릭터 34명**(여성 19): 22차에 여성 5 + 남성 1 추가. 일러스트를 받은 캐릭터는 스프라이트 색(`artPalettes.js`)과 성별 베이스가 그림에 맞춰진다. 인사 명단은 S→D 등급 섹션(보유 수/확률 표시).
- **일괄 레벨업**: 파티 관리 창의 각 영웅에 +1 / +10, 상단에 "일괄 +5"(전원 최대 5레벨, 라운드 로빈) / "일괄 최대"(골드 소진까지 균등). 리본의 "자동 합계"(가장 싼 것 반복)와 보완 관계.

## 6-17. 24~25차: Excel식 카드 · 레벨 되돌리기 · 별 기반 강화 한계

- **카드 디자인**: 카드를 "데이터 레코드"로. 상단 셀(128×150)에 일러스트를 자르지 않고 contain으로 전체 표시(미보유는 흑백), 아래 두 줄의 셀에 이름 | 등급 태그(정사각 셀 채움), ★★★☆☆ | Lv. 얇은 셀 테두리, 왼쪽 등급색 3px, 그림자·둥근 모서리 제거. 배지도 정사각.
- **레벨 되돌리기**: 상세 창에 -1 / -10 / 초기화(전액 환급) / +1 / +10. 환급률 `LEVEL_REFUND = 1.0` — 골드를 카드 사이에서 자유롭게 옮기는 것이 목적(레벨은 "투자", 카드 뽑기·별이 진짜 성장).
- **강화 한계 = 별**: ★1 10 · ★2 20 · ★3 30 · ★4 40 · ★5 50, 각성 +10(최대 60). 메인 영웅은 직급 티어별 10/20/30/40/50. 상세 창에 `강화 +x / 한계 y (★z)` 표시, 한계에 닿으면 승급·각성 안내 토스트. → 중복 카드로 별을 올려야 강화가 열리므로 뽑기의 가치가 커진다.

## 6-18. 30차: 부문 시너지 · 연속 출근 · 오류 도감 · 명단 필터

- **부문 시너지** (`src/data/divisions.js`): 34명의 부서를 7개 부문(경영지원 · 시설·복지 · 인사 · 재무·감사 · 기술 · 영업·마케팅 · 임원)으로 묶었다. 파티에 같은 부문 2명 → 파티 ATK +5%, 3명 이상 → ATK +12% · HP +6%; 탱커/근접/원거리/힐러 4역할을 모두 편성하면 HP +10%. 시너지는 파티에 있는 카드에만 적용(명단의 대기 카드는 기본 수치). → "가장 센 5장"이 아니라 "같은 부문으로 묶기"가 편성 고민을 만든다. 파티 작업 창과 명단 헤더에 시너지 칩 표시.
- **부문 고유 특성** (31차, `PERKS`): 같은 부문 2명 이상이면 ATK 사다리에 더해 부문별 특성이 파티 전체에 켜진다. 경영지원 골드 +8% · 시설복지 초당 회복 +1% · 인사 복귀 시간 -30% · 재무감사 보스 피해 +15% · 기술 스킬 쿨다운 -15% · 영업마케팅 전원 치명타 +8% · 임원 스킬 위력 +20%. 특성 이름은 부서 정체성에서 따와 "어느 부문으로 묶을지"가 전략이 되게 했다. EntityManager는 rebuildParty/refreshHeroStats 시점에 perks를 캐시한다.
- **연속 출근** (`state.login`): 어제에 이어 도장을 찍으면 연속 일수 +1, 하루 건너뛰면 1일로 리셋. 2일째부터 하루당 보석 +20, 최대 7일치(+140). 일일 업무 시트에 "연속 출근 N일째 → 보석 +x" 예고.
- **오류 도감** (`state.bestiary`, 시트 `오류_도감`): 처치한 몬스터를 기본 종류(색상 변형은 하나로) + 보스 3종 = 29칸으로 집계, 엘리트 처치 수 별도. 미발견은 실루엣 + `???`. 새 종류 등록 시 토스트, 업적 「오류 도감」(5/12/20/29 → 보석 30/80/200/500)과 연동.
- **명단 자동 필터**: 인사_명단 시트에 Excel 데이터 › 필터처럼 등급/역할/부문/보유(보유·미보유·파티)/정렬(등급·ATK·Lv·★·이름) 드롭다운. 등급순 이외의 정렬에서는 등급 구분 행을 숨긴다. 명단 표에 부문 열 추가.

## 6-19. 32차: 무료 백엔드 · 세이브 타당성 · 광고 래퍼

- **왜 서버인가**: 사용자 질문 "로컬 파일로 다이아 조작 가능하지 않나, 서버 비용 없이 백엔드 가능한가". 결론: 조작 자체는 못 막고(싱글 방치형은 본인 손해), 막을 가치가 있는 곳은 **공유되는 것**(순위표, 클라우드 저장)뿐. Cloudflare Worker + D1 무료 티어로 서버 비용 0.
- **타당성 검사** (`src/core/plausibility.js`, 클라이언트·서버 공용): (1) 보유 레벨의 누적 업그레이드 비용 ≤ totalGold×1.02 + 500 (레벨은 환급형이므로 "현재 보유 레벨"이 곧 소비 골드의 하한), (2) gems + totalPulls×90 ≤ 1000 + maxCleared×150 + 일수×1500 + 상자×8 + 6000, (3) totalKills ≤ playSeconds×6 + 2000, (4) 클리어한 일반 스테이지×20×0.9 − 100 ≤ totalKills, 보스 스테이지×0.9 − 2 ≤ bossKills, (5) playSeconds ≤ 계정 나이×1.1 + 900. 헤드리스 10분 시뮬레이션이 모든 경계 안에 있음을 테스트로 고정.
- **순위 점수**: 지분×1000 + 최고 클리어 + min(0.999, DPS/1e9). 회사 이전한 플레이어가 스테이지가 낮아져도 뒤로 밀리지 않게.
- **광고**: AdSense H5 Games Ads(보상형 adBreak). 서버 검증(SSV)이 없는 API이므로 보상은 방치 골드만, 하루 3회. 광고 시청은 /v1/ad에 기록해 나중에 이상 패턴을 볼 수 있게만 한다.
- 열린 질문: 순위표 이름 필터(욕설), 광고 승인 후 실제 노출 빈도(30s 힌트) 조정.

## 6-20. 33차: 오늘의 픽업 · ★ 미리보기

- **오늘의 픽업** (`src/data/pickup.js`): 날짜 키로 S 1장 · A 1장을 정해 모든 플레이어가 같은 카드를 본다. 등급 순서를 고정 셔플해 하루에 한 칸씩 돌기 때문에 A 6장은 6일, S는 S 카드 수만큼의 주기로 모두 한 번씩 픽업된다. 해당 등급이 나오면 `PICKUP_RATE = 0.5`로 픽업 카드가 뽑히고, 나머지 50%는 기존처럼 균등. 등급 확률·천장은 그대로라 기대 보석 비용은 변하지 않고 "누구를 노릴지"만 생긴다. 뽑기 결과·이력에 픽업 표시.
- **★ 미리보기**: 상세 창 HP 아래에 다음 별의 ATK·HP와 배율(`STAR_MULT`), 새 강화 한계를 한 줄로. 승급 버튼을 누르기 전 "얼마나 세지는지"가 보인다.

## 6-21. 34차: 사무용품 스킬 연출

- 스킬 타입마다 회사 소품 연출을 덧붙였다(기존 링/슬래시 유지): 강타 = 대상 위에 붉은 「결재」 도장이 내려찍힘(1.6→1 스케일 슬램) · 범위 정리 = 시전자에서 적 라인으로 서류 폭풍(14장, 회전·낙하) · 경영 지시 = 적 구간 위로 셀 격자가 떨어지며 대각선 웨이브로 셀이 점등 + 전원 「반려」 도장 · 팀 싱크 = 영웅 머리 위 실적 막대 차트 4개가 솟음 · 웰니스 데이 = 커피컵과 김. 모두 fillRect/strokeRect 기반이고 위치는 정수로 반올림해 픽셀 격자를 지킨다(Renderer #drawEffects: stamp/papers/grid/chart/coffee).

## 6-22. 35차: 야근 모드 (일일 서바이벌)

- **규칙** (BALANCE.OVERTIME): 하루 1회, 60초. 최고 스테이지 +3 난이도의 웨이브가 7마리씩, 엘리트 35%, 웨이브 간 이동 0.4초(평소 1.6초), 상자 없음. 처치 1건 보석 2, 엘리트 6, 상한 400 + 강화 카드 (페이즈×2). 스테이지 진행·처치 카운터는 건드리지 않고 골드는 빌려온 스테이지 기준으로 지급. 전원 번아웃 시 도전 실패 처리 없이 재정비 후 계속.
- **왜**: 방치형에 하루 한 번 "손으로 지켜볼" 60초를 만들고, 파티가 강해질수록 같은 60초에 처치가 늘어 성장을 체감하게 한다. 보석 상한 400은 10행 가져오기 절반 정도로, 일일 업무 보석(≈220)과 합쳐 이틀에 한 번 10연을 노릴 수 있는 수준. 개인 최고 처치·누적 횟수는 stats, 업적 「야근 수당」(1/7/30/100회).
- HUD: 좌상단 카운트다운 바(10초 미만 붉게 점멸) + 처치/엘리트/예상 보석. 종료 시 「야근 결과 보고서」 모달.

## 6-23. 36차: 회사 업그레이드 재설계 (사용자 요청)

- **성과급 제도 → 보석 드롭 확률**: 골드 +5%/Lv를 버리고 "처치 시 보석 드롭 확률 +0.1%/Lv (최대 50 → +5%)"로. 기본 드롭 0.5%, 보석 1개, 엘리트 ×3 (BALANCE.GEM_DROP). 초당 1.5처치 기준 만렙에서 약 0.08보석/초 ≈ 시간당 300보석(10행 가져오기 1/3) — 능동 플레이 보상, 방치 골드에는 영향 없음. 골드 배율은 이제 영업 마인드·도감·지분·부문 특성만.
- **비용**: 회사 업그레이드는 파티 전원에 걸리므로 영웅 1명 레벨업보다 훨씬 비싸야 한다는 사용자 판단. base 50→300~400, growth 1.25→1.26~1.28: 커피 Lv30 ≈ 66만 골드 ≈ 영웅 Lv100 한 단계, 의자 Lv100 ≈ 3조(후반 골드 싱크). Lv0 가격도 영웅 Lv30 레벨업보다 높다(테스트 고정).
- 파티 작업 창 표에 현재 수치(공속 +x%, 드롭 x.x%, HP +x%)를 항목 설명 옆에 표시.

## 6-24. 38차: 스킬 시스템 확장 (사용자 요청: 영웅·스킬 고도화)

- **새 스킬 4종** (SKILLS): 화상 `burn`(모든 적에게 4초간 초당 ATK×p, 갱신형·비중첩) · 보호막 `barrier`(6초, 파티 공용 흡수량 ATK×p, 시전자가 강할수록 두꺼움) · 가속 `haste`(5초 파티 공속 +p%) · 처형 `execute`(단일 ×p, 대상 HP 30% 미만이면 2배, 대상은 저체력 보스/엘리트 우선). 기존 5종과 합쳐 9종.
- **영웅별 스킬 이름** (`skill.name`): 정체성이 맞는 10명을 새 타입으로 옮기고 고유 이름을 붙였다 — 개발팀장 「핫픽스 배포」, 매크로 엔지니어 「매크로 폭주」, 박박사 「가설 검증」(화상) · 윤대리 「조항의 방패」, 총무팀장 「비품 창고 성벽」(보호막) · 유주임 「보도자료 스프린트」, 헬프데스크 「재부팅」(가속) · 한대리 「저격 보고」, CTO 「리팩토링」(처형). 나머지는 타입 기본 이름.
- **AI**: 보호막은 이미 보호막이 남아 있으면 아끼고(HP 90% 미만·적 3+·보스), 가속은 팀 싱크와 같은 조건, 처형은 항상 시전(저체력 대상 탐색). 상태 표시는 좌하단 상태줄에 팀 싱크/가속/보호막 한 줄씩.

## 6-25. 39차: 뽑기 고도화 (사용자: "뽑기가 주된 시스템")

- **3일 배너**: 픽업이 매일 바뀌면 노릴 수 없어서 `PICKUP_DAYS = 3`. 배너 인덱스 = floor(일수/3), 남은 날짜를 시트에 표시.
- **모집 포인트 (스파크)**: 1행 가져올 때마다 +1, 만료 없음(블루아카는 배너 종료 시 소멸이지만 캐주얼 방치형이라 유지). 현재 픽업 S는 150, A는 60 포인트로 직접 교환(미보유 → ★1 영입, 보유 → 조각 +10). 150행 = 보석 13,500 ≈ 천장(100행) 이후 "누구를 고를지"의 보험. 포인트 바·교환 버튼은 픽업 카드 아래.
- **신입 환영**: 세이브 첫 10행은 A 대신 **S 확정**(`FIRST_TEN_GUARANTEE`). 시작 보석 1000으로 첫 10연을 돌리면 얼굴이 되는 S가 하나 생긴다.
- **투명성**: 확률표에 픽업 실효 확률(S 0.5% · A 3.5%) 병기, 누적 등급별 뽑기 수(stats.pullGrades) 표시, 이력에 픽업/교환 표기.

## 6-26. 40차: Google 로그인 계정 (사용자: "로컬 빼고 로그인 시스템")

- **구조**: Google Identity Services 버튼(스크립트 1개, SDK 없음) → ID 토큰 → Worker가 tokeninfo로 검증 → 30일 세션 토큰. 기기 id+secret 방식은 제거. 로그인하지 않으면 브라우저 저장만(게스트) — 배포된 게임이 백엔드 없이도 돌아가야 하므로 강제 로그인은 하지 않는다.
- **저장본 충돌**: 로그인 직후 서버 저장본(플레이 시간·최고 스테이지)과 브라우저 진행이 다르면 두 카드로 보여 주고 고르게 한다. 같으면 조용히 통과, 서버에 없으면 즉시 업로드.
- **제목 표시줄**: 계정 이름/아바타가 Google 프로필로 바뀌고 클릭하면 파일 › 옵션 › 계정. 순위표에 프로필 사진 표시.
- **광고**: AdSense는 github.io 하위 경로를 사이트로 받지 않는다(최상위 도메인 필요) → 커스텀 도메인이 선행 조건. 코드(Ads.js)는 준비돼 있고 publisherId만 채우면 된다.

## 6-27. 41차: 블루아카식 캐릭터 시스템 (호감도 · 사내 메신저 · S 스플래시 · 말풍선)

- **호감도** (BALANCE.AFFECTION): 파티에 있는 카드가 처치마다 +1(보스 +15), 하루 1회 간식(골드, 최고 스테이지 기준 40처치분) +45. 레벨 사다리 xp = 60×1.45^(L-1), 최대 Lv10(누적 ≈ 3,900 ≈ 파티 상주 1~2일). 레벨당 해당 카드 ATK/HP +1%(최대 +10%). Lv3 사무실 비화, Lv5 개인 메시지(사내 메신저 1:1 방), Lv10 MAX. 좋아하는 캐릭터를 파티에 오래 두는 이유를 만든다.
- **사내 메신저** (`src/data/story.js`, `profilesExtra.js`): 구역(Phase)마다 에피소드 1개, 총 10화. 채팅 로그를 엑셀로 내보낸 형태(시간 | 보낸 사람 | 메시지)로 65초 간격 타임스탬프, 0.65초마다 한 줄씩 등장, 처음 읽으면 보석 +30(총 300). 호감도 Lv5 카드는 1:1 방이 목록에 추가된다(profile.line → 나의 고민 → line2).
- **S 스플래시**: 뽑기에서 S가 뒤집힐 때 0.7초 뒤 풀 일러스트가 대화상자 위로 슬라이드 인 + 이름·닉·대사, 클릭 또는 3.6초 후 닫힘. 블루아카 3★ 연출의 축소판.
- **말풍선**: 도전 시작(60%)·첫 클리어·보스 처리 시 파티 중 한 명이 profile.line을 픽셀 말풍선으로 3.2초 말한다(Renderer #drawBubbles, 정수 좌표·2px 테두리).

## 6-28. 42차: 스킨 · 스킬 강화 · 상세 창 탭

- **스킨** (`src/data/skins.js`): 영웅·직급마다 2종. 「퇴근 사복」은 호감도 Lv10 도달 시 자동 해금(재킷 색을 보색 방향으로 150° 돌린 팔레트), 「회사 정장」은 보석 300(네이비+금). 스킨은 def.skin으로 붙어 스프라이트 팔레트(heroSkins의 ART_PALETTES 위에 덮어씀)와 카드 일러(매니페스트에 `<id>__<skin>` 항목이 있으면)를 바꾼다. 캐시 키에 스킨 id 포함. 일러가 없으면 기본 일러를 그대로 쓰므로 GPU 쿼터가 돌아오면 skins.js의 prompt로 스킨 일러만 추가 생성하면 된다.
- **스킬 강화** (BALANCE.SKILL_LEVEL): ★2 해금 이후 강화 카드로 Lv1~5, 레벨당 위력 +10%·재사용 대기 -3%. 비용 = 등급별 단가(D8 C12 B20 A32 S50) × 다음 레벨 → S 만렙 750장. 강화 카드의 두 번째 사용처(첫 번째는 카드 강화/각성).
- **상세 창 탭**: 정보(수치 표) · 스킬·스킨(특성/스킬/★/스킨) · 프로필·호감도. 창이 길어져 스크롤이 생기던 것을 정리. 마지막 탭은 기억.

## 6-29. 43차: 출장 · 사원 앨범 · 카드 메모

- **출장** (BALANCE.DISPATCH): 파티 밖 대기 카드 최대 3명을 4시간 보낸다. 하루 2회. 보상 = 보석 20 + 등급별(D3 C5 B8 A12 S20) + 강화 카드(페이즈×1) + 출장자 호감도 +60. 출장 중인 카드는 파티에 넣을 수 없고 자동 편성에서도 빠진다. "뽑았지만 파티에 못 넣는 카드"에 쓸모를 주는 장치(블루아카 카페/스케줄 역할). 타임스탬프 기반이라 게임을 꺼도 진행.
- **사원_앨범 시트**: 일러스트 갤러리. 미보유는 흑백, 장착한 스킨 일러가 있으면 그 버전과 스킨 태그. 클릭하면 원본 라이트박스.
- **카드 메모**: 명단 카드에 마우스를 올리면 Excel 메모(노란 노트, 빨간 삼각형) 모양으로 일러 썸네일 · 닉네임 · 부서 · 대사가 따라다닌다.

## 6-30. 44차: 손으로 짠 페이퍼돌 도트 (GPU 없이 일러 → 도트)

- 사용자 제안: "AI 대신 네가 일러를 보고 직접 도트를 찍자". 41장 일러를 컨택트 시트(scripts/artSheet.mjs)로 보고 인물마다 머리 모양·색, 피부, 복장 종류·색, 소품, 후광 색을 스펙으로 적었다(`src/data/dollSprites.js` DOLLS). 스펙 → 16×28 픽셀 조립(머리 10종, 복장 9종, 소품 15종, 스커트/바지) → 실루엣 1px 어두운 외곽선 → 9프레임(대기 4·걷기 4·피격 1) 스트립. 0x72 베이스 재색 방식(heroSkins)보다 우선하며, 스킨 팔레트는 재킷/바지/머리 색으로 통과.
- 검수는 scripts/dollSheet.mjs(캔버스 없이 Node에서 PNG 렌더)로 6배 확대해 확인. 걷기 프레임은 다리 교차 + 1px 바운스.
- 45차 피드백 "너무 길쭉해서 귀여운 맛이 없다" → 치비 비율로 재조립: 머리 10×10(y 6..15), 몸통 6줄·다리 4줄·발 2줄, 어깨 폭 12, 2×2 눈 + 볼터치. 안경은 회색 테로 눈이 보이게. 46차: 오른쪽 3/4 시선(눈·입·코를 오른쪽 절반에, 뒷머리는 왼쪽) + 몸통 4줄·다리 3줄로 더 짧게 — 머리:몸 ≈ 10:9.
- 한계: 정면 3/4 고정 포즈라 공격 모션은 기존과 같이 프레임 [0,6,0] 스윙으로 처리. 세부 디테일(무늬·장식)은 2px 격자에서 생략.

## 6-31. 45차: 매출 인센티브 · 방출 환급 · 한꺼번에 수령 · 검토 시트 2열

- **매출 인센티브** (TEAM_UPGRADES.sales): 골드 +1%/Lv, 최대 50 → +50%. 사용자 요청대로 성과급(보석 드롭)과 함께 "드라마틱하지 않게" — 둘 다 후반 기간을 늘리는 완만한 곡선. 비용 350×1.27^Lv.
- **방출 환급**: 카드를 방출하면 강화 카드 외에 그 카드 레벨에 투자한 골드를 LEVEL_REFUND(100%)로 돌려준다(레벨 -1/초기화와 같은 규칙). 파티 카드는 원래 방출 불가.
- **한꺼번에 수령**: 검토 시트 헤더 버튼 하나로 완료 업무·전체 완료 보너스·업적(모든 단계)·복귀한 출장을 수령. 버튼에 건수 배지, 툴팁에 내역.
- **검토 시트 레이아웃**: 왼쪽 열(일일 업무·업적·마일스톤 = 수령형), 오른쪽 열(출장·야근·광고 = 행동형, 초록 좌측 띠). 블록마다 제목 바를 두어 "중간에 껴 있어 구분이 안 간다"는 피드백을 반영.

## 6-32. 47차: S 희귀화 · S 신규 4명 (사용자: "S는 더 어렵게, 성능 비례로")

- **확률**: D 45→48.5 · C 30 · B 17→16 · A 7→5 · S 1→0.5 (%). S 천장 100→120, A 천장 50 유지, 10연 A 보장·첫 10연 S 보장·모집 포인트(150)는 그대로. 기대 S 1장당 보석 약 12,000~13,500 → 천장/스파크가 실질 경로. 성능 비례: S 기본 ATK 20 vs D 6(3.3배)인데 확률은 97배 차이 → "S를 뽑는 순간"이 사건이 되게.
- **S 신규 4명**: 전략실장 서하늘(원거리·팀 리더십·궐 「전략 브리핑」), AI연구소장 루나(힐러·점심 시간·「시스템 복구」 45%), 노조위원장 강철(탱커·철벽 멘탈·보호막 「단체 협약」 14), 화이트해커 제로(근접·날카로운 지적·처형 「제로데이」 6). 부서→부문: 전략기획실=임원, AI/보안연구소=기술, 노동조합=인사. 프로필·비화·개인 메시지·스킨·도트·일러 프롬프트까지 데이터는 완성, 카드 일러는 SVG 임시(GPU 복구 시 생성). 도감 업적 상한 34→38.
- 미형 원칙(사용자): 등급이 높을수록 확실히 예쁘고 화려하게, D도 못생기지 않게 — 프롬프트의 GRADE/OUTFIT 규칙과 도트 S 트림으로 반영.

## 6-33. 49차: 로그인 필수 · API 절감 · Excel 표 스타일 (사용자: "보안 최우선, 통신 줄이고, 더 엑셀처럼")

- **로그인 게이트**: 서버가 설정된 배포에서는 첫 접속에 Excel 계정 로그인 화면(초록 배경 카드 + Google 버튼)이 뜨고, 로그인 전에는 시뮬레이션이 멈춘다(GameManager.paused). 세션이 만료돼 401이 오면 게이트가 다시 뜬다. 계정 저장본이 브라우저보다 새롭거나 진행이 많으면 로드 후 시작. 방치 보상은 "실제로 시작하는 저장본" 기준으로 계산. localhost에서만 `?guest=1`로 우회(개발용).
- **API 절감**: 자동 저장 2분→5분, 그것도 gems/cards/gold/roster/party/stage 등 변경 이벤트가 있었을 때(dirty)만. 탭 숨김·닫힘 시 keepalive PUT 1회. 순위표는 5분 캐시(새로 고침 버튼은 강제). 서버는 사용자별 PUT 20초 간격 제한(429), 크기 상한, 스냅샷+변화량 검사. → 활성 플레이어 1명당 하루 PUT ≈ 100~300회, Worker 무료 10만 요청/일이면 수백 명 동시 활성까지 여유.
- **레이아웃**: 모든 표 머리글을 Excel 표 스타일(초록 #217346 채움·흰 글자·줄무늬 행·호버), 구역 제목은 병합 셀처럼 초록 띠(행동형 블록은 파랑), 시트 탭은 Excel 탭 색(전투 초록·명단 파랑·가져오기 보라·업무 주황·메신저 핑크·앨범 금·도감 회색·차트 청록)으로 한눈에 구분.

## 6-34. 50차: 로딩 체감 · 일러 경량화 · 파티 창 Excel화 · 마일스톤 수동 수령

- **로딩**: 카드 일러 41장 PNG(45 MB)를 부팅 때 전부 기다리던 것이 템포 문제의 원인. Pillow(scripts/thumbs.py)로 416×608 WebP 썸네일(총 1.7 MB)을 만들어 카드·명단·앨범은 썸네일을 쓰고, 원본 PNG는 라이트박스·S 스플래시에서만 연다(매니페스트 `{ file, thumb }`). 일러 로드는 부팅을 막지 않고 백그라운드로 진행, 8장마다 명단을 다시 그린다.
- **보이는 로딩**: Excel 시작 화면 스플래시(초록 배경, 진행 바, 단계 문구: 스프라이트 → 저장 확인 → 계정 확인/로그인 대기 → 통합 문서 열기 → 준비 완료), 상태줄 ☁ 표시(저장 중/불러오는 중 스피너, 마지막 저장 시각, 로그인 필요), 로그인 화면의 "계정 확인 중…".
- **파티 관리 창**: 시너지 칩 → Excel 표(시너지 | 인원 | 효과), 흰 배경·회색 제목 바·얇은 도구 버튼으로 다른 시트와 같은 문법.
- **마일스톤 수동 수령** (사용자 요청): 도달 시 자동 지급 대신 "달성 ✓ · 수령" 행으로 표시하고 한꺼번에 수령에도 포함. 도달 알림은 배너·로그로 1회.

## 6-35. 51차: 전투 연출 — 스킬 대사 · 보스 컷인 강화

- **스킬 대사** (`src/data/quips.js`): 스킬 타입별 짧은 대사 3개(강타 "결재 도장, 찍습니다!", 처형 "최종 검토, 반려." 등)를 시전 순간 말풍선 1.8초로. 궐은 기존 컷인(profile.ult)이 있어 제외. 이미 말하고 있으면 덮어쓰지 않는다.
- **보스 컷인**: 띠 위아래에 노란 위험 테이프(대각 스트라이프), 스프라이트 3× + 발 그림자, HP/ATK 미리보기 텍스트. 보스가 라인에 도착하면 보스별 대사(긴급 티켓 "긴급! 긴급! 긴급!", 야근 좀비 "퇴근… 하고 싶으면… 야근…", 갑질 오우거 "단가 더 깎아!")를 붉은 말풍선으로 3초 + 화면 흔들림. 말풍선 시스템을 몬스터(보스)까지 확장.

## 6-36. 52~54차: 순위표 이름 필터 · 절친 프레임 · 도넛 차트 · 코치 마크 · 가독성/정렬 피드백

- **이름 필터**(plausibility.sanitizeName, 서버·클라 공용): 제어/제로폭 문자 제거, 공백 정리, 욕설·운영자 사칭 단어 → "익명 사원", 16자. 서버는 비어 있으면 Google 이름으로 대체.
- **절친**: 호감도 Lv10 카드는 핑크 프레임 + 「♥절친」 태그(각성 금색 프레임과 겹치면 금색 유지, 태그는 둘 다), 상세 창 헤더에 칭호.
- **통계_차트**: 도감 구성(등급별 보유) · 파티 부문 편성 도넛 차트 2개 추가(Excel 도넛 스타일, 중앙 합계, 우측 범례).
- **첫 출근 가이드**: 새 저장본에서 환영 창을 닫으면 도움말 풍선 4단계(가져오기 → 파티 관리 → 검토 → 다음 단계 도전)가 대상을 하이라이트하며 안내. 건너뛰기 가능, 1회.
- **피드백 반영**: 배너/HUD 흰 글자에 어두운 외곽선(야근 HUD는 더 진한 박스), 페이퍼돌 외곽선을 몬스터와 같은 근검정(#1b1d25)으로 통일하고 후광은 외곽선 뒤에 그려 색 유지, 파티 창 표를 제목 띠와 같은 폭으로 정렬·인원 열 줄바꿈 제거·출현 몬스터 3마리 한 줄, 스킨 미리보기 4배 확대, ★승급 → 「한계 돌파」(중복 카드 = 조각 = 돌파 재료임을 문구로), 일일 초기화 카운트다운.

## 6-37. 55차: 로스터 확장 38→50 · 신규 S 일러 완성

- GPU 쿼터가 실제로는 남아 있었다(도트용 호출만 실패했고 일러 호출은 정상). 신규 S 4명 일러 생성 완료(cso/ai_lead/union_chief/hacker), 도트 스펙을 생성된 일러에 맞춰 수정(라벤더+네이비·금, 금발 스파이크+주황 재킷, 네이비 후드 원피스+금 줄).
- **신규 12명** (여 9 · 남 3): D 인턴 민지(힐러·행운·응원 메시지) · 보안팀 양사원(탱커·보호막) · 사내 우편 조사원(근접·가속) / C QA 이주연(원거리·처형) · 안내데스크 고은(힐러) · 사내 강사 석훈(탱커·버프) / B 번역팀장 지아(원거리·범위) · 비서실장 윤서(힐러·버프) · 물류팀장 배철(탱커·보호막) / A CDO 하린(원거리·화상) · CCO 미소(힐러·커피 수혈) / S 비서실 총괄 유하(근접·가속 45). 새 부서: 해외사업팀·고객경험본부(영업마케팅), 물류팀(시설복지), 데이터본부(기술). 프로필·비화·개인 메시지·궐 대사·도트·스킨·일러 프롬프트·배경 포함. 도감 업적 상한 50.
- 미형 원칙 적용: D도 귀엽게(리본 트윈테일 인턴, 포니테일 보안), 등급이 오를수록 정장·금장·연출 강화.

## 7. 전투 / 성장 (1차 결정 유지)

- 영웅 ATK = `floor(base × 1.10^(Lv-1) × StarMult × (1 + 0.04×강화))`, HP 성장 1.08.
- 몬스터 ATK = `floor(1 × 1.13^(Stage-1))`, 보스 HP ×8 / ATK ×3 / 골드 ×20, 30초 제한 → 실패 시 후퇴 루프.
- 슬롯 front(1)/mid(2)/back(2), 탱커 우선 어그로 85%, 힐러는 HP 60% 미만 아군 우선 회복.
- (9차 이후) 라인 전투 규칙은 6-3 참고 — 근접 영웅은 대열 위치와 무관하게 도착한 몬스터를 모두 칠 수 있다.
- 파티 전멸 시 1스테이지 후퇴(Phase 1-1에서는 진행도 유지), 사망 영웅 8초 후 부활, 초당 2% 자연 회복.

## 8. 열린 질문

1. 부장 분기 선택을 되돌리는 "전직" 기능(카드 비용)이 필요한지.
2. 광고 SDK: 어느 네트워크(AdMob/Unity Ads/웹은 AdSense H5 등)를 쓸지 정해지면 `UIManager.playAd`만 교체.
3. ~~일일 업무 외 업적(누적) 시스템 필요 여부.~~ → 10차에 구현.
4. 아트: 현재 코드 생성 치비 픽셀. 실제 일러스트가 생기면 `sprites.js`의 `cardCanvas/portraitCanvas`에서 이미지로 교체 가능.

## 6-38. 56차: 주인공 승진 트랙 분기 · 주인공 성별 고정

- **트랙 분기 시점 이동**: 기존 인턴→사원→대리→과장→(부장 3분기) 선형 트리를 인턴→사원→**트랙 선택(영업/재무/총무)**→대리→과장→부장으로 바꿈. `MAIN_JOBS`에 `track` 필드, `MAIN_TRACKS` 상수 추가. 트랙별 역할·특성·스킬이 대리부터 갈라짐(영업=근접 강타/치명→팀 리더십, 재무=원거리 광역/영업 마인드→보고서 특화, 총무=탱커/조항의 방패→경영 지시).
- **구버전 저장 이관**: `senior`/`manager`(근접 강타)는 `state.migrate`에서 `sales_senior`/`sales_manager`로 자동 매핑(같은 티어).
- **주인공은 남성 고정**: 기존 김대리 일러가 여성으로, 총무부장이 백발 노인으로 나와 인물 연속성이 깨져 있었음. 모든 직급 프롬프트에 'young man, short black hair'를 명시하고 도트 스펙도 검은 단발·같은 피부색으로 통일. 신규 6장(각 트랙 대리·과장) + 총무부장 재생성.
- 테스트: 트랙별 사슬(2→3→4, B→A→S, 역할 1종, 다른 트랙으로 못 넘어감) + 이관 검증. 시뮬레이션 테스트는 재무 트랙으로 끝까지 승진.

## 6-39. 57차: 일러 얼굴 규칙 — 크게, 밝게

- 사용자 원칙: 선글라스 같은 소품은 괜찮지만 **얼굴이 음영에 묻히거나 작게 보이는 일러는 매력이 없다**. 61장을 컨택트 시트로 전수 점검해 18장(barista, courier, pivot, cto, coo, ceo, legal_yoon, cmo, founder, union_chief, security_yang, secretary_yun, logistics_bae, cdo, cco, chief_of_staff, chairwoman, finance_manager)을 재생성.
- 프롬프트 공통 규칙(`scripts/genCardsHF.mjs`): `cowboy shot` → `medium shot, upper body, waist up, face focus, soft even front lighting, bright face`. 네거티브에 `shadowed face, low key lighting, full body, wide shot, distant, small face` 추가. S 등급 기본 배경의 `dramatic lighting` 제거, 야경/역광 배경(cto, union_chief, security_yang, chairman)을 밝은 배경으로 교체.
- 검수 방법: `IDS=a,b node scripts/artSheet.mjs out.png 8 180`으로 시트를 만들고 눈으로 확인 → 문제 카드만 `SEED=<n> --force`로 재생성 → `python scripts/thumbs.py --force` → `--manifest` → `extractPalettes`.

## 6-40. 58차: 상세 창 5탭 분리 — 정보 · 스킬 · 스킨 · 프로필 · 호감도

- 사용자 요청: 정보/스킬·스킨/프로필·호감도 3탭을 각각 따로 두고 레이아웃을 강화. 모든 탭을 Excel 표(`dt-table`, 머리글 열 + 값 + 컨트롤 + 힌트) 형식으로 통일.
- **정보**: 기존 스탯 표 + 특성 행. **스킬**: 스킬명(계열/고유 명칭) · 효과 · 위력(기본 × 스킬 Lv × ★4 × 각성 분해) · 대기 시간 · 지속 · 스킬 Lv(강화 버튼) · ★ 성장 사다리(메인은 승진 효과). **스킨**: 장착 중(기본으로 되돌리기) · 해금 n/2와 조건 · 적용 범위 + 6× 도트 미리보기 그리드(96×168). **프로필**: 인사 기록 카드(이름/별명/부서+부문 특성/직무/소개/한마디). **호감도**: 하트·Lv·경험치 바·보너스·간식 행 + 해금 사다리(Lv 3 비화 · Lv 5 개인 메시지 · Lv 10 절친/사복/핑크 프레임).
- 탭 버튼에 배지: 스킬 `Lv n`, 스킨 `장착 스킨명`, 호감도 `♥n`. 저장된 탭 키 `growth`는 `skill`로 이관.
- (57차 후속) 남은 12장 재생성 완료. HF `/call` API가 숨기던 오류를 queue 프로토콜로 읽어 진짜 원인(요청당 GPU 90초 vs 잔여 쿼터 부족) 확인. 스크립트에 쿼터 풀 로테이션 추가: `.hf_token` + `.hf_tokens`(한 줄에 하나, git-ignored) + 익명(IP) 풀을 순서대로 시도하고, 모두 소진되면 Space가 알려 준 "Try again in H:MM:SS"만큼 대기. `HF_ANON=1`은 익명만 사용.

## 6-41. 59차: 광고 공급자 추상화 (AdSense · AppLixir · custom)

- 사용자 질문: 애드센스 승인 전에 Monetag / Adsterra / AdinPlay / AppLixir 중 먼저 붙일 것이 있는지. 결론: **보상형 전용인 AppLixir만 후보**. Monetag·Adsterra의 팝언더/전면은 엑셀 위장 UX를 깨고, 같은 도메인에서 애드센스 심사 중 침습적 광고가 돌면 승인에 불리. AdinPlay는 웹게임 검수·트래픽 조건이 있어 지금 단계엔 이르다.
- `src/ui/Ads.js`를 공급자 추상화로 재작성: `window.EXCEL_HEROES_ADS`가 `{ publisherId }`면 AdSense H5, `{ provider: 'applixir', applixir: { zoneId, devId, gameId } }`면 AppLixir SDK(`invokeApplixirVideoUnit`, 상태 `ad-watched`/`ad-rewarded`=시청, `ad-interrupted`/`sys-closing`=취소, 그 외=불가), `{ provider: 'custom', custom: { load, showRewarded } }`면 임의 네트워크. 호출부(`showRewarded`)와 일일 상한(BALANCE.AD.perDay)은 그대로. AppLixir 상태명은 대시보드 스니펫과 대조 필요.

## 6-42. 60차: 스킬 일러 컷인 · 보스 개성 강화 · 컷인 가독성

- **스킬 컷인**: 일반 스킬 발동 시 왼쪽 위(보스 바 아래)에 일러스트 명찰 카드(64px 얼굴 크롭 + 이름 + 스킬명 + 계열) 1.4초. 새 발동이 오면 교체(한 장만). 필살기 컷인은 3× 도트 대신 140×196 일러 카드(등급색 프레임, 살짝 기울임). 일러가 아직 안 로드되면 도트로 대체. 스킨 장착 시 스킨 일러 우선.
- **보스**: 도트 2×→3×(96×108, 캔버스 128×116)로 일반 몬스터(2×)보다 확실히 크게. 컷인 보스 도트는 6×. 보스마다 고유 스킬을 데이터(`BOSSES[].specials`, every N번째 공격)로 정의하고 전투·'!' 예고·오류_도감 설명이 같은 데이터를 읽음:
  - 긴급 티켓: 35% 불덩이 + **티켓 폭주**(5번째: 불덩이 3연발 각 60%, 서로 다른 영웅)
  - 야근 좀비 부장: **야근 지시**(3번째: 앞 두 명 90%) + **야근 강요**(6번째: 파티 공격 속도 ×0.7, 4초 — HUD에 ▼ 표시)
  - 갑질 거래처 오우거: **갑질 발구르기**(4번째: 전원 50%) + **서류 투척**(6번째: 맨 뒤 영웅 140%)
  두 스킬이 겹치는 턴은 더 드문 쪽이 발동. 격노(50% HP) 유지.
- **가독성**: 보스/필살기 컷인의 전면 어둡게 처리가 하단 데미지 숫자·이름·HP를 가렸음 → 위 280px에만 그라데이션 오버레이. 말풍선은 보스가 커진 만큼 위로.

## 6-43. 61차: 광고 보상 메뉴 — 고정 지급 5종

- 사용자 지적: "광고 보고 2배"는 게임을 꺼 두는 쪽이 이득이 되는 구조라 잘못됨 → **방치 배율 삭제**. 광고 보상은 전부 고정 지급.
- `BALANCE.AD_OFFERS`: 유휴 골드 1시간분(3회) · 보석 +15(2회) · 강화 카드 +10(2회) · 출장 즉시 복귀(1회, 출장 중일 때만) · 야근 모드 추가 1회(1회, 오늘 야근을 마친 뒤). 하루 총 9회(`AD.perDay` = 합). 일별 카운터 `daily.ads[key]`, 야근 추가는 `daily.overtimeExtra`.
- UI: 일일 업무의 광고 블록이 표(보상/지급/오늘/버튼)로 바뀌고, 리본 '광고 보상' 버튼은 같은 표를 모달로 연다. 복귀 정산 창의 버튼은 "유휴 골드 1시간분 받기"(고정). 광고 공급자 호출 이름은 offer key(gold/gems/…)로 넘겨 서버 ad_views에 종류가 남는다.
- 보안 메모: 보석·카드 광고 보상은 클라이언트 지급이지만 하루 상한(보석 30, 카드 20)이 작고 plausibility 예산(하루 1500 보석) 안에 있다.

## 6-44. 62차: A/B 등급 일러 품질 상향 · 얼굴 가림 방지

- 사용자 지적: "COO가 CCO 미소에 비해 급 차이가 너무 난다". 원인은 등급 태그였다 — S는 `gold trim, sparkles, light particles, glowing`인데 A는 `executive, luxurious details`뿐이라 A 카드가 한 등급 아래로 보였다.
- **등급 태그 재조정**(`scripts/genCardsHF.mjs` GRADE): 전 등급에 `beautiful detailed face` 추가, A에 `gold accents, sparkles, light particles`, B에 `soft light particles`. D/C는 그대로 담백하게 두어 등급 사다리는 유지.
- **개별 묘사 강화 5명**: CFO(보라 스리피스 + 금 시계줄, 떠다니는 금색 숫자), CTO(차콜 블레이저 + 청록 홀로그램 코드), CHRO(모브 정장 + 진주·금 브로치, 흩날리는 꽃잎), 디자인팀장(베레모 + 빛나는 태블릿, 물감 튀김), 기획팀장(데님 재킷 + 부채꼴 기획서, 떠다니는 포스트잇). 배경도 밝게/보케 추가.
- **마스크 차단**: CDO·QA 이 등에서 하관을 마스크·스카프가 덮는 결과가 반복돼 네거티브에 `face mask, surgical mask, mouth mask, scarf over face, veil, covered mouth` 추가. CDO 프롬프트도 밝은 미소로 교체.
- 도트 5종을 새 일러에 맞춰 재배색: CFO 보라 정장, CTO 스파이키 + 청록 트림, 기획팀장 파란 보브 + 흰 블라우스, 디자인팀장 분홍 곱슬 + 파란 베레, CHRO 크림 재킷 + 올리브 스커트.

## 6-45. 63차: 스킬 컷인 재연출 · CDO 재생성

- 사용자 지적: "왼쪽 상단에 샥 뜨고 마는 건 맛이 없다". 작은 명찰 카드를 **비스듬한 평행사변형 대형 컷인**으로 교체: 왼쪽 화면 밖에서 스피드 라인과 함께 슬램 인(0.12초) → 유지 → 다시 왼쪽으로 슬라이드 아웃. 등급색 띠를 뒤에 한 겹 깔아 입체감, 초상 옆에 기울어진 명찰(영웅 이름 + 스킬명 22px 외곽선). 표시 1.6초, 초상은 y 92~278로 전투선(318)을 가리지 않음.
- CDO 재생성: 마스크가 하관을 덮던 문제 해결(네거티브 보강 + 밝은 미소 프롬프트). 금장식·반짝임이 들어가 A 티어와 균일.

## 6-46. 64차: 스킨이 도트의 옷을 갈아입힌다 · 도트 페인터 버그 2건

- 기존 스킨은 팔레트만 바꿔서, 사복을 입혀도 같은 정장 실루엣이었다. `skins.js`에 도트 오버라이드(`doll: { outfit, dropAcc, addAcc }`)를 추가: 퇴근 사복 = 여성 카디건 / 남성 후디(헤드폰), 업무용 액세서리(넥타이·사원증·명찰·금장식·멜빵·안전모·앞치마) 제거. 회사 정장 = 정장 + 금장식 + 명찰. `buildDollStrip`/`dollPixels`는 팔레트만 받던 것을 스킨 전체를 받도록 확장(기존 팔레트 인자도 그대로 동작).
- **버그 1 — 금장식이 보이지 않았다**: `trim`을 몸통 바깥 열(x 3 / x 12)에 찍었는데 그 자리는 외곽선이 덮어써서, 금장식을 가진 임원 12명 모두 장식이 없었다. 한 픽셀 안쪽(x 4 / x 11)으로 이동.
- **버그 2 — 카디건이 정장과 동일**: `case 'suit': case 'cardigan':`으로 묶여 있어 여성 사복 스킨이 기본과 구분되지 않았다. 카디건은 앞이 트인 넓은 안감 + 부드러운 밑단으로 분리.

## 6-47. 65차: 프롤로그 — 운석, 고리, 그리고 야근

- 사용자 요청: 첫 구글 로그인 때 오프닝 스토리를 일러와 함께 보여줄 것. 설정: 운석이 떨어지고 몬스터가 생겼는데, 우리 회사 직원들 머리 위에만 고리가 생겨 특수 능력을 얻었다 → 지구를 구할 수 있는 건 이 회사뿐.
- **고리의 조건을 엑셀 농담으로 확장**: 고리는 아무에게나 생기지 않는다. "남의 망가진 표를 대신 고쳐 본 사람, 밤을 새워 참조를 이어 붙여 본 사람 — 그러니까 야근을 해 본 사람에게만." 야근 모드·오류(몬스터)·#REF! 설정과 이어진다.
- 7장면(`src/data/prologue.js`): 분기 마감 D-1 → 창밖의 빛 → 충돌 → 오류의 등장 → 머리 위의 고리 → 각성 → 인사 명단("…제가요?" / "오늘도 정시 출근입니다").
- 재생: 전체 화면 플레이어(`UIManager.showPrologue`), 장면당 일러 1장 + 내레이션이 한 줄씩 페이드 인, 이전/다음/건너뛰기 + 키보드(←/→/Enter/Esc), 진행 점. 첫 로그인 직후 1회 자동(`settings.prologueSeen`), 이후 사내_메신저 시트의 「프롤로그 다시 보기」로.
- 일러: `node scripts/genCardsHF.mjs --scene` (1216×832 가로, `assets/story/<id>.png` → thumbs.py가 webp 변환, 게임은 webp만 로드). PNG 원본은 git 제외.
- 주의: 이 모델은 캐릭터 모델이라 인물 없는 장면 프롬프트는 빈 거리·추상 파편으로 나온다. 장면마다 `1boy`/`1girl` 주체를 세우는 구도가 정답(3차 프롬프트). 현재 패널은 1~3차가 섞여 있어 쿼터 회복 후 `--scene --force`로 전량 재생성 예정.
- (65차 후속) HF ZeroGPU 쿼터 진단: 이 Space는 **이미지 크기·스텝과 무관하게 항상 90초를 요구**한다(832×1216 28스텝, 640×896 8스텝 모두 "90s requested"). 그래서 풀에 82~87초가 남아 있어도 한 장도 못 뽑는다. 재시도 대기가 "마지막으로 거절한 풀"의 초기화 시각을 쓰고 있어 가장 빠른 풀(6시간 후)을 놓치고 23시간을 기다리던 문제를 수정 — 이제 회전 중 본 것들 중 **가장 빠른 초기화**까지만 기다린다.
- (65차 후속 2) 프롤로그 패널 재생성 완료. 교훈 3가지: (1) 이 모델은 인물 없는 장면 프롬프트를 빈 배경으로 그린다 → 장면마다 `1boy`/`1girl` 주체를 세운다. (2) "화면을 가득 채운다/압도한다" 같은 표현은 추상적인 색 폭발로 뭉개진다 → 구체적인 동작과 배경으로 쓴다. (3) 인물 + 대형 사건을 한 컷에 넣으면 실패한다 → **리액션 클로즈업**(사건이 얼굴에 비친 빛·눈에 반사된 기호)으로 대체하면 같은 beat를 훨씬 잘 전달한다. 고리는 스타일 태그에서 빼고 5~7번째 패널에만 붙인다(`HALO_SCENES`), 앞 패널은 네거티브로 차단 — 고리가 생기는 순간이 이야기의 전환점이기 때문.

## 6-48. 66차: 비품(장비) 시스템 — 스테이지에서 나오는 유일한 성장 축

- 영웅 성장 축이 레벨·★·강화·각성·스킬Lv·호감도로 촘촘한데 전부 **재화를 쓰는** 축이었다. 비품은 **스테이지를 깨야 나오는** 축이자, 후반에 남아도는 골드의 두 번째 배출구다.
- **슬롯 4종**(한 슬롯당 한 스탯이라 "공격형/방어형 사원"으로 읽힌다): 키보드=공격력 · 의자=체력 · 모니터=스킬 위력 · 사원증=공격 속도. 등급은 카드와 같은 D~S, 이름도 등급마다 다르다(지급품 키보드 → 장인이 깎은 키보드).
- **수치**: 기본 D 2% / C 4% / B 7% / A 11% / S 16%, 레벨당 기본값의 +12%, 최대 Lv 10. S 4부위 풀강이 한 스탯 기준 약 +64%로 ★나 각성보다 작다 — 비품은 보조 축이지 지름길이 아니다.
- **드롭**: 일반 클리어 35%, 보스 클리어 100% + 2회 굴려 좋은 쪽 채택(보스에 계속 도전할 이유). 등급 가중치는 페이즈에 따라 위로 이동. 창고 120칸.
- **운용**: 상세 창에 「비품」 탭 추가(4슬롯 + 슬롯별 창고 목록). 강화는 골드(현재 스테이지 기준으로 스케일), 분해도 골드로 환급, 착용 중인 비품은 분해되지 않음. D·C급 일괄 분해 버튼.
- 첫 비품이 떨어지면 토스트로 탭을 알린다(안 그러면 아무도 못 찾는다). `plausibility.checkSave`에 창고 상한·레벨 상한·"클리어 수보다 많은 비품" 검사 추가.

## 6-49. 67차: 카드 · 스테이지 · 밸런스 동시 고도화

### 카드 — 역할 = 스킬 하나였던 문제
- 힐러 12명 중 11명이 같은 `heal`을 썼다. 누구를 뽑아도 같은 연출, 같은 판단이었다. **새 스킬 5종** 추가:
  - `cleanse` 스트레스 해소 — 회복 + **보스 둔화 해제** + 3초 가속 (야근 좀비 부장의 「야근 강요」에 대한 답)
  - `revive` 인사 복구 — **쓰러진 동료를 즉시 복귀**시키는 유일한 수단 (부활 타이머를 기다리지 않는다)
  - `drain` 성과 회수 — 전체 피해 + 입힌 피해의 40%를 파티 회복 (딜과 힐을 겸함)
  - `chain` 참조 연쇄 — 적 3명에게 연쇄, 링크마다 ×0.7 (넓은 웨이브 보상, 보스에는 약함)
  - `taunt` 총대 메기 — 5초간 **모든 근접·원거리 공격을 자신이 대신 받고** 받는 피해 감소 (탱커가 처음으로 탱커다워짐)
- 재배치: ai_lead→cleanse, chro→revive(사람을 살리는 임원), design_lead→haste, reception_go→buff, cleaner→cleanse(청소), cdo→drain, vlookup→chain(VLOOKUP이 참조를 따라간다), guard·trainer_seok→taunt. 결과 분포 heal 11→6, 최대 사용 스킬 6개로 평탄화.

### 스테이지 — 한 페이즈가 같은 전투 10번이던 문제
- 변형 2종(야근 러시·감사 기간)에 3종 추가: **정전**(파티 공격 속도 -20%, 몬스터 HP -20%, 골드 ×1.35) · **납기 압박**(몬스터 ATK +35%, 이동 +20%, 골드 ×1.5) · **단체 민원**(몬스터 +3, HP -35%, 골드 ×1.25).
- 배치를 페이즈마다 다르게(`PHASE_MODIFIERS`, 5종 로테이션): 1층은 변형 2개, 5층은 4개로 층이 올라갈수록 변주가 늘어난다. 보스 스테이지에는 변형이 붙지 않는다.
- 모든 변형은 난이도를 올리는 대신 골드를 더 준다 — 변형 스테이지에 머무를지 넘어갈지가 선택이 된다.

### 밸런스 — 측정 결과
- 자연 진행 시뮬레이션(자동 강화 on): 보스 10 @19분(Lv 29) · 20 @35분(Lv 46) · 30 @55분(Lv 61) · 40 @89분(Lv 77). 보스 40 HP ≈ 253k, 해당 시점 DPS ≈ 9.5k → **제한 30초에 근접**해 긴장감이 유지된다. 과도한 레벨(Lv 120)에서는 2초에 끝나지만 그건 진행 곡선상 도달하지 않는 지점.
- **보스 첫 클리어 = 비품 확정 드롭, 최소 B급**(4회 굴려 최고값). 보스를 계속 두드릴 이유를 비품 축과 연결.
- 변형 골드 배수가 야근 모드에서도 맞도록 `s.stage` → `combatStage()`로 교정.

## 6-50. 68차: 썸네일과 원본이 어긋나던 문제 — 캐시 무효화

- 사용자 보고: "썸네일하고 미리보기가 다른 경우가 있다". 디스크 전수 검사(`scripts/thumbcheck.py`, 두 파일을 16×16으로 줄여 픽셀 비교) 결과 **161장 전부 일치** — 파일은 멀쩡했다.
- 진짜 원인은 **브라우저 캐시**였다. 일러를 재생성해도 파일명이 그대로라, 카드 그리드는 캐시된 옛 썸네일(webp)을 쓰고 상세 창은 새로 받은 원본(png)을 쓰는 상태가 생긴다. 오늘만 카드 20여 장을 같은 이름으로 갈아 끼웠으니 필연.
- 해결: 매니페스트에 **빌드 스탬프**(`version` = 디스크상 가장 최근 아트 파일의 mtime)를 넣고, 모든 아트 URL에 `?v=<stamp>`를 붙인다. 일러가 바뀔 때만 스탬프가 움직이므로 평소에는 캐시가 그대로 유지되고, 재생성하면 썸네일·원본·프롤로그 패널이 한 번에 갱신된다.
- 재발 방지: `scripts/thumbcheck.py`(`--fix`로 자동 재생성)와, 매니페스트의 모든 항목이 실제 파일을 가리키는지 + **썸네일이 원본보다 오래되지 않았는지** 검사하는 테스트 추가.
- (68차 후속) 프롤로그 자막이 일러를 가린다는 피드백: 어둡게 덮는 그라데이션이 화면 아래 **62%를 불투명도 .97까지** 채우고 있었다. 자막이 실제로 차지하는 띠(스테이지 높이의 58~94%)에 맞춰 **44% · 최대 .74**로 줄이고, 가독성은 글자에 1px 검정 외곽선(4방향 그림자) + 부드러운 글로우로 받치게 바꿨다. 결과적으로 그림의 위쪽 절반 이상이 그대로 보이면서 문장은 밝은 하늘 위에서도 읽힌다.

## 6-51. 69차: 보안 점검 — 순위표 위조 · 워커 하드닝

실제 코드를 훑어 확인한 것과 고친 것.

### 이미 막혀 있던 것
- SQL은 전부 바인딩 파라미터(주입 불가), 세션은 256비트 난수 불투명 토큰(30일, 만료 시 삭제), Google ID 토큰을 서버에서 `aud`/`iss`/`exp`까지 검증, CORS 허용 목록, 저장 크기 상한 256 KB, 순위표 이름 필터, 인증은 쿠키가 아니라 Bearer 헤더라 CSRF 해당 없음, 순위표 렌더링은 텍스트 노드라 XSS 없음.

### 구멍이었던 것 (수정함)
1. **지분(shares)이 무검증** — `boardScore`는 지분에 ×1000 가중치를 주는데 `checkSave`가 지분을 전혀 보지 않았다. 손으로 고친 저장 하나로 1위가 가능했다. → 회사 이전은 `PRESTIGE.minCleared` 스테이지를 다시 밟아야 하고 스테이지는 처치 수로, 처치 수는 플레이 시간으로 이미 묶여 있으므로 **"처치 수가 허용하는 이전 횟수 × 최고 스테이지 기준 지분"** 을 상한으로 두었다. 델타 검사도 추가: 이전 횟수가 늘지 않았는데 지분만 늘면 거부.
2. **DPS가 클라이언트 자유 입력** — 순위표에 그대로 표시됐다. 점수 가중치는 작지만 "DPS 9경"이 뜰 수 있었다. → 최고 스테이지와 지분으로 계산한 넉넉한 상한(`maxPlausibleDps`)으로 클램프.
3. **저장 속도 제한이 메모리 전용** — Cloudflare가 새 아이솔레이트를 주면 초기화됐다. → 이미 읽어 오는 `saves.updated_at`으로 **영속 검사**를 추가(추가 쿼리 0회).
4. **세션 무한 누적** — 로그인할 때마다 행이 쌓이고 만료된 것도 남았다. → 로그인 시 만료 세션 삭제 + 계정당 최신 10개만 유지.
5. **광고 엔드포인트가 무제한 INSERT** — 카운터일 뿐인데 DB를 무한히 불릴 수 있었다. → 하루 40건 상한, 초과 시 429.
6. **500 응답이 내부 오류 메시지를 노출** — SQL 오류 문구가 그대로 나갈 수 있었다. → 서버 로그로 보내고 응답은 일반 메시지.

### 남은 위험 (기록)
- 세션 토큰이 D1에 평문 저장. DB가 유출되면 세션 탈취 가능 → 해시 저장이 정석이나 마이그레이션 필요, 다음 차수 후보.
- 단일 플레이어 방치형이라 로컬 저장 조작 자체는 막을 수 없다. 위 검사들은 **공유 순위표와 클라우드 저장**에 말이 안 되는 값이 오르지 못하게 하는 것이 목적.

## 6-52. 70차: 주인공은 파티 고정 · 승진은 졸업

- 사용자 요청: 주인공은 무조건 기본 장착, 승진 기준을 더 빡세게.
- `toggleParty`가 주인공 해제를 거부한다(파티에 다른 사원이 있어도). 주인공은 슬롯이 아니라 플레이어 본인.
- 승진 조건에 **현재 직급의 레벨과 강화 한계**를 추가: 강화 카드·스테이지에 더해 `MAIN_PROMOTE_LEVEL`(15/40/80/140)과 그 티어의 강화 한계(+10/+20/+30/+40)를 모두 채워야 한다. 승진 패널이 네 조건을 각각 초록/빨강으로 보여 주고, 미달 시 "승진은 구매가 아니라 졸업입니다" 안내.

## 6-53. 71차: 카드 방출 안전장치 · 전투력 · 삽입 시트 중앙 배치

- **방출은 여분만**: 조각이 0개인 카드(한 번만 뽑은 카드)는 방출되지 않는다. 파티 배치·즐겨찾기 카드도 제외. 막힌 이유를 상세 창 버튼 옆에 그대로 적는다.
- **일괄 방출**: 인사_명단 필터 바에 등급 선택(D~S 이하) + 「여분 방출」. 위 보호 규칙을 그대로 적용하고, 실행 전 몇 장·누가 나가는지 확인 창에 보여 준다.
- **전투력**: 등급과 ★의 우열이 이미 교차하는데(기본 ATK 기준 A★3 = 24 > S★1 = 20, B★5 = 30.8 > S★2 = 25) UI에 그 사실이 없었다. `heroView.power = ATK×2 + HP÷10` 한 숫자로 등급·★·레벨·강화·비품을 모두 반영해 명단 표의 열, 상세 창 첫 행, 정렬 옵션(전투력순)에 노출.
- **일반(D) 카드도 스킬을 쓴다**: 스킬 해금은 등급이 아니라 ★2 기준(`SKILL_UNLOCK_STAR`)이라 D 카드도 ★2부터 스킬을 시전한다. ★1에서만 평타.
- **삽입 시트 레이아웃**: 픽업 배너·뽑기 버튼·확률표·이력이 모두 좌측 절반에 몰려 있었다. 배너를 **상단 중앙(최대 760px, 금색 테두리·중앙 정렬 헤더)** 으로 올리고 그 아래 뽑기 버튼 두 개를 중앙 배치, 확률표와 이력은 그 밑에서 2열로 나눴다.

## 6-54. 72차: 비품 운용 — 세트 효과 · 자동 장착

비품을 넣고 나서 드러난 문제: 아이템 120개를 영웅 50명 × 4슬롯에 손으로 끼워야 했고, 슬롯마다 최고 수치를 고르는 것 말고는 판단할 거리가 없었다.

- **세트 효과**: 4부위를 채우면 모든 능력치 +3%, 네 부위를 **같은 등급**으로 맞추면 등급별 +4/7/11/16/24%. 최고 수치 하나를 쫓을지 세트를 맞출지가 실제 선택이 된다(S 풀세트 +24%는 A 키보드 하나의 차이를 뒤집는다).
- **자동 장착**: 두 후보 — ① 슬롯별 최고 성능 ② 완성 가능한 같은 등급 세트 중 최강 — 를 **합계로 비교해 더 강한 쪽**을 고른다. 다른 사원이 착용 중인 비품은 후보에서 제외하므로 자동 장착이 동료 장비를 빼앗지 않는다. 「이 사원」/「파티 전체」 두 버튼, 파티 순서대로 앞사람이 먼저 고른다.
- **가시성**: 파티 관리 표에 `비품 n/4` 열(4부위면 초록, 0이면 빨강, 마우스를 올리면 세트 이름과 보너스). 상세 창 비품 탭에 세트 행 추가.
- 업적 「비품 관리대장」(4/20/60/120개) 추가.

## 6-55. 73차: 세션 토큰 해시 저장 (6-51의 남은 위험 해소)

- 69차 점검에서 "세션 토큰이 D1에 평문 저장 — DB가 유출되면 세션 탈취 가능"을 남은 위험으로 적어 두었다. 이번에 해소.
- `sessions.token`에 **SHA-256 해시만** 저장한다. 클라이언트가 들고 있는 토큰은 그대로 쓰이고, 테이블에는 재사용 가능한 값이 남지 않는다.
- **무중단 이관**: 해시로 찾지 못하면 평문으로 한 번 더 조회하고, 찾으면 그 행을 해시로 즉시 바꿔 쓴다. 기존 로그인 세션이 끊기지 않는다.
- 테스트: 로그인 후 테이블에 원본 토큰이 없고 해시가 있는지, 그 토큰으로 인증이 되는지, 평문 행이 한 번 통과한 뒤 해시로 바뀌는지 검증.
- 배포 시 워커 재배포 필요: `npx wrangler deploy --config backend/wrangler.toml`

## 6-56. 74차: 파티 자동 편성을 시너지 인식형으로

- 기존 `autoParty`는 **ATK 순으로 다섯 명**을 뽑았다. 하지만 부문 시너지(2·3명)는 파티 전체에 곱해지고, 역할 균형 HP 보너스와 힐러 유무는 생존을 좌우하며, 비품 세트까지 들어온 지금은 개인 수치만 보는 편성이 확실히 틀린 답이 됐다.
- **편성 점수**(`partyScore`) 도입: 전투력 합 × (1 + 시너지 ATK + 시너지 HP/2) × (1 + 시너지 세트 수 × 4%) × (힐러 있으면 ×1.08) × (전 역할 보유 ×1.06).
- 탐색: 탱커·힐러 + 최강 카드로 시드를 만든 뒤 **한 명씩 교체하며 점수가 오르는 동안 반복**(최대 3패스, 후보는 상위 18명). 탱커와 힐러는 점수로 거래되지 않는 **하드 제약** — 로스터에 있으면 반드시 편성에 남는다(앞줄 없는 파티는 숫자와 무관하게 죽는다).
- 실측: 같은 로스터에서 편성 점수 5,715 → 18,756, 부문 시너지 2종 확보, 탱커·힐러 유지.

## 6-57. 75차: 밸런스 실측 패치 · 보석 코드

### 측정 (추측이 아니라 시뮬레이션 결과)
| 항목 | 결과 |
| --- | --- |
| 자연 진행(자동 강화·편성·장착) | 10단계 12분 · 40단계 48분 · 70단계 165분 (비품 도입 전 10단계 19분 → 초반 약 40% 단축) |
| 보석 수급 (레벨이 스테이지를 따라갈 때) | 깊이와 무관하게 시간당 약 1,750 — 정상 |
| 보석 수급 (첫 클리어 소진 후) | 30분당 1,513 → 138 → 124 → 84으로 붕괴 |
| 회사 이전 | 60단계 재등반 41분 (최초 104분) + 영구 +42% — **루프 자체는 정상 작동** |

### 고친 것
1. **반복 클리어 보석이 고정값이었다**: 페이즈 7의 클리어는 페이즈 1보다 몇 배 오래 걸리는데 보상은 똑같이 1(보스 10)이었다. `gemsForClear(stage)`로 페이즈당 일반 +1 / 보스 +5. 85단계 반복은 9 / 50이 된다. plausibility 보석 예산도 깊이에 비례해 올려(하루 1,500 + 페이즈×900) 정직한 깊은 저장이 거부되지 않게 했다.
2. **벽에 막혀도 회사 이전을 알려 주지 않았다**: 측정상 이전은 재등반 2.5배 가속 + 영구 보너스로 확실히 이득인데, 게임 어디에도 그 사실이 없어 플레이어는 이전이 해결하라고 만든 벽을 그냥 갈았다. `prestigeAdvice()`가 **자격이 있고 실제로 막혔을 때만**(다음 단계 승산이 안전선 미만이거나 자동 진행 대기) 작업 창에 구체적 이득("지분 +6 = 파티 ATK·골드 +18%, 재등반은 처음보다 훨씬 빠름")을 띄운다. 「나중에」는 15분 스누즈.

### 보석 코드 (요청 기능)
- `src/data/codes.js`의 표 한 곳에 코드를 적으면 끝. 삽입 시트 뽑기 버튼 아래 입력란에서 사용한다. 대소문자·공백·하이픈 무시, 저장당 1회, `until`로 기간 제한 가능.
- **설계상 클라이언트 측**이다(코드 목록이 게임과 함께 배포됨). 비밀이 아니라 배포용 쿠폰으로 다루면 된다. 사용한 코드는 `state.redeemed`에 남고, plausibility 보석 예산이 이를 반영해 정상 사용이 순위표 검사에 걸리지 않는다.
- 현재 등록된 코드는 예시 `EXCELHEROES`(보석 300 · 강화 카드 20) 하나. 나머지는 사용자가 정해 추가.
- (75차 후속) 코드 확정: **`helloheros`** · **`ref!`** 각 보석 3,000, **계정당 1회**. 대소문자·공백·하이픈은 무시하지만 `!` 같은 코드의 일부인 문장부호는 유지된다.
- "계정당 1회"는 저장 파일로는 보장할 수 없어(초기화하면 다시 받게 됨) **서버에 기록**한다: D1 테이블 `redemptions (user_id, code)` + `POST /v1/redeem`. 로그인 상태면 서버가 최종 판정(이미 사용 시 409), 로그아웃 상태면 로컬 기록으로 임시 처리하고 다음 로그인 때 서버가 우선한다. 로그인 게이트가 필수라 실제 서비스에서는 항상 서버 경로.
- 배포 절차: `npx wrangler d1 execute excel-heroes --remote --file=backend/schema.sql --config backend/wrangler.toml` (redemptions 테이블 생성) → `npx wrangler deploy --config backend/wrangler.toml`

## 6-58. 76차: 재화 곡선 구조 교정 (실측) · A급 연출 · 연속 뽑기

### 재화 실측 (자동 강화 off, 순수 수입 기준)
| 단계 | 골드/h | 보석/h | 카드/h |
| --- | --- | --- | --- |
| 10 | 169,120 | 820 | 288 |
| 40 | 17,277,592 | 972 | 760 |
| 70 | 1,316,043,472 | 1,116 | 1,316 |

골드 수입은 78배 늘지만 **파티 +10레벨 비용은 26만 배** 늘어난다. 같은 한 걸음이 10단계에서 0.08시간, 70단계에서 2.63시간 — 33배 나빠진다.

### 원인 (수식)
- 한 스테이지를 더 깨려면 ATK가 몬스터 HP를 따라가야 한다 → 필요한 레벨 = `log(1.18) / log(1.10)` = **1.74레벨/스테이지**
- 그 레벨들의 비용 증가 = `1.12^1.74` = **×1.218/스테이지**, 골드 수입 증가 = **×1.15/스테이지**
- 괴리 **×1.0587/스테이지** → 50스테이지에 17.3배, 100스테이지에 300배 가난해진다. 중후반이 죽는 원인.

### 교정
`UPGRADE_COST_GROWTH 1.12 → 1.105`, `GOLD_GROWTH 1.15 → 1.16` (양쪽에서 좁힘)
→ 괴리 **×1.0253/스테이지**, 50스테이지 3.5배 · 100스테이지 12.1배. 벽은 남기되(프레스티지 유인 유지) 중후반이 숨 쉴 수 있다.
테스트도 상수를 하드코딩하던 것을 `BALANCE`에서 읽고 **괴리 범위 자체를 검증**하도록 바꿔(>1.005, 50단계 <6배) 다음 튜닝이 곡선을 조용히 망가뜨리지 못하게 했다.

### 연출·편의 (요청)
- **A급 등장 배너**: S만 전체 화면 스플래시가 있어 A는 그냥 지나갔다. A는 카드가 뒤집히는 순간 하단에 짧은 띠(초상 + 등급 + 이름 + 신규/조각)로 1.5초. 등급 차이는 연출 차이로 유지.
- **연속 뽑기**: 결과 창에 「1행 더」/「10행 더」와 보유 보석을 붙였다. 창을 닫고 시트로 돌아가는 왕복이 사라진다. 보석이 모자라면 버튼이 비활성.

## 6-59. 77차: 재화 곡선 검증 · 가챠가 멈추던 진짜 이유

### 6-58 교정이 실제로 먹혔는지
| | 교정 전 | 교정 후 |
| --- | --- | --- |
| 70단계 도달 | 165분 | **76분** |
| 4시간 후 | 79단계 | **90단계** |

10시간 시뮬에서 20단계 구간 소요는 22 → 28 → 37 → 71 → 126분. **벽은 사라지지 않고 60단계 부근에서 100단계 부근으로 밀렸다**(의도한 결과 — 프레스티지 유인은 유지).

### 보석 수급원 분해 (벽에 막히지 않은 상태, 시간당)
| 단계 | 총 | 처치 드롭 | 클리어·보스 |
| --- | --- | --- | --- |
| 25 | 885 | 126 | 759 |
| 85 | 1,020 | 90 | 930 |

**약 80%가 클리어 기반**이라, 벽에 막혀 클리어가 멈추면 가챠도 같이 멈춘다. 벽에서도 계속 도는 유일한 수급원이 처치 드롭이므로 이걸 깊이에 비례시켰다: `GEM_DROP.amount`에 `amountPerPhase 0.5`(페이즈 2개마다 +1). 결과 드롭 기여 90~126 → 291~735, 총 수급 885~1,020 → 1,098~1,689.

### 결론: 가챠가 멈추던 진짜 이유는 수급이 아니라 **이전을 안 하는 것**
같은 10시간을 두 방식으로 돌린 결과:
| | 벽을 그냥 간다 | 권유대로 이전 |
| --- | --- | --- |
| 최고 단계 | 179 | 190 |
| 지분 | 0 | 226 |
| **뽑기** | **40회** | **140회** |
| **보유 영웅** | **24/50** | **39/50** |
| S 획득 | 1 | 3 |

이전을 하면 빠른 클리어 구간으로 돌아가 클리어 기반 수급이 되살아나고, 그 결과 뽑기가 3.5배가 된다. 76차에 넣은 회사 이전 권유 배너가 이 루프를 여는 열쇠였다(시뮬에서 권유 표시 O 확인). plausibility 보석 예산도 새 수급에 맞춰 하루 `2000 + 페이즈×1800`로 올렸다.

## 6-60. 78차: A급 5명 추가 (7 → 12)

- 등급 분포가 D 12 · C 12 · B 10 · **A 7** · S 9로, 플레이어가 실제로 파티를 꾸리는 A 티어가 가장 얇았다. 5명 추가해 12명으로.
- 새 카드는 전부 **얇은 스킬·특성을 메운다**: CRO 위기관리(탱커/철벽/**총대 메기**) · CPO 제품총괄(원거리/보고서 특화/**참조 연쇄**) · IR 담당임원(원거리/영업 마인드/**성과 회수**) · 노무사 서린(힐러/커피 수혈/**인사 복구**) · 사업개발실장(근접/**전체 회신**/범위 정리). 결과: chain 1→2, drain 1→2, revive 1→2, taunt 2→3, splash 1→2, lifesteal 2→3.
- 부서 5종 추가(리스크관리실·IR팀 → 재무, 노무팀 → 인사, 제품본부 → 기술, 사업개발팀 → 마케팅)로 부문 시너지 선택지도 넓어졌다.
- 일러 15장(본체 5 + 스킨 10) 생성, 도트 스펙·0x72 폴백·프롬프트·팔레트까지 반영. 인재 도감 업적 최종 단계 50 → 55.
- 픽업 로테이션 테스트가 28일 고정 창이라 A가 늘면 깨졌다 → **로스터 크기에 맞춰 창이 자라도록** 수정(카드를 더 추가해도 보장이 유지되는지 계속 검증된다).

## 6-61. 79차: 플레이 피드백 반영 — 조작감·위장 모드 편성·투자 회수

플레이테스트 피드백 네 가지를 그대로 받았다.

1. **"위장 모드(Esc)에서 상세보기-배치가 안 된다"** — 실제 버그였다. Esc는 카드 그리드를 숨기고 텍스트 표만 남기는데 그 표의 행에는 클릭 핸들러가 없어, 위장 상태에서는 파티를 바꿀 방법이 아예 없었다. 행 클릭 → 상세 창, 그리고 표 안에 **배치/해제 버튼 열**을 추가. 배치된 사원은 행 왼쪽에 초록 줄.
2. **"뭐가 진행 중인지 모르겠다"** — 상태 표시줄에 지금 무엇을 하는 중인지 한 줄로 항상 적는다(이동 중 / 도전 중 n/20 처치 / 보스전 제한 30초 / 반복 사냥 / 승산 대기 / 야근). 자동 진행이라도 "무엇을 기다리는지"가 보이면 관전이 된다.
3. **"자동 편성하면 레벨 올린 D가 먼저 들어간다"** — 전투력만 보면 맞는 답이지만 플레이어 의도와 다를 수 있어 리본에 **최소 등급** 선택을 붙였다(전체/C/B/A/S 이상). 그 등급으로 파티가 안 차면 전체 로스터로 되돌아간다.
4. **"저등급에 넣은 골드가 낭비 아니냐"** — 구조를 보면 낭비가 **아니다**: `upgradeCost`는 등급이 아니라 레벨만 보고 `LEVEL_REFUND`는 1.0이라, D에 넣은 60레벨을 빼서 A에 옮기면 한 푼도 잃지 않는다. 진짜 문제는 **그 환급을 자동으로 해 주는 것이 없었다**는 점(카드를 하나씩 열어 「초기화」를 눌러야 했다). `reclaimBenchLevels()`를 만들고 자동 편성이 끝날 때 호출한다 — 벤치로 내려간 사원의 골드는 즉시 회수되고, 주인공·파티원·즐겨찾기·출장 중인 카드는 건드리지 않는다.

- 덤: 브라우저 `window.confirm` 4곳을 전부 게임 모달(`#askConfirm`)로 교체했다. OS 대화상자가 뜨는 순간 Excel 위장이 깨지기 때문.
